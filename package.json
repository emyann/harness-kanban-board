{
  "name": "hkb-cli",
  "version": "0.2.2",
  "description": "hkb \u2014 harness kanban board. A portable, frugal Hermes-style kanban for coding agents; GitHub Issues is the default backend, any harness (Claude Code, Copilot CLI, Codex) is a worker.",
  "type": "module",
  "bin": {
    "hkb": "bin/hkb.js"
  },
  "files": [
    "bin",
    "src",
    "web",
    "skills",
    "commands",
    "hooks",
    "templates",
    ".claude-plugin",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test",
    "test:core": "node --test test/*.test.ts",
    "test:legacy": "node --test test/*.test.js",
    "lint": "for f in $(find bin src scripts -name '*.js' -o -name '*.mjs'); do node --check \"$f\" || exit 1; done && npm run typecheck",
    "typecheck": "if [ -x node_modules/.bin/tsc ]; then node_modules/.bin/tsc --noEmit; else echo 'hkb: type check skipped \u2014 no local typescript. Run `npm install` first (a devDependency; hkb itself still runs with an empty node_modules).' >&2; fi",
    "smoke": "node scripts/smoke-pack.mjs"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "prisma": "7.10.0",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22.13"
  },
  "keywords": [
    "kanban",
    "github",
    "github-issues",
    "agents",
    "cli",
    "claude-code",
    "copilot",
    "codex",
    "hermes"
  ],
  "license": "MIT",
  "author": "emyann",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/emyann/harness-kanban-board.git"
  },
  "homepage": "https://github.com/emyann/harness-kanban-board#readme",
  "bugs": {
    "url": "https://github.com/emyann/harness-kanban-board/issues"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.261",
    "@prisma/adapter-better-sqlite3": "7.10.0",
    "@prisma/client": "7.10.0"
  }
}
