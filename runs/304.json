{
  "attempts": [
    {
      "attempt": 1,
      "bg": true,
      "ended_at": "2026-09-03T10:54:22.722Z",
      "heartbeat_at": "2026-09-03T09:54:07.187Z",
      "host": "YRND1-GG",
      "job": "a4edbcda",
      "job_stopped": true,
      "lock_sha": "e93a6109a019fcec2e7a25a6a906131a07e29dc0",
      "log": ".kanban/logs/304-1.log",
      "outcome": "reclaimed",
      "pid": null,
      "profile": "claude",
      "reason": "reclaimed after 3615s",
      "session_id": "a4edbcda-035e-45ed-bd61-a261d9f5613a",
      "started_at": "2026-09-03T09:54:07.187Z",
      "transcript_path": "/home/yrnd1/.claude/projects/-home-yrnd1-projects-harness-kanban-board--claude-worktrees-kb-304-1/a4edbcda-035e-45ed-bd61-a261d9f5613a.jsonl",
      "wt": "kb-304-1"
    },
    {
      "attempt": 2,
      "bg": true,
      "ended_at": "2026-09-03T10:58:03.217Z",
      "heartbeat_at": "2026-09-03T10:56:11.042Z",
      "host": "YRND1-GG",
      "job": "9378e90e",
      "kind": "transient",
      "lock_sha": "e93a6109a019fcec2e7a25a6a906131a07e29dc0",
      "log": ".kanban/logs/304-2.log",
      "outcome": "blocked",
      "pid": null,
      "profile": "claude",
      "reason": "Operator: restarting so the recovery note above reaches the brief — it posted 47s after this attempt claimed, so attempt 2 never saw it and would have redone attempt 1's hour of work from scratch. Nothing wrong with the attempt.",
      "session_id": "9378e90e-d93d-4f41-85f9-d8f99f134310",
      "started_at": "2026-09-03T10:56:11.042Z",
      "transcript_path": "/home/yrnd1/.claude/projects/-home-yrnd1-projects-harness-kanban-board--claude-worktrees-kb-304-2/9378e90e-d93d-4f41-85f9-d8f99f134310.jsonl",
      "wt": "kb-304-2"
    },
    {
      "attempt": 3,
      "bg": true,
      "ended_at": "2026-09-03T11:18:21.563Z",
      "heartbeat_at": "2026-09-03T10:58:55.833Z",
      "host": "YRND1-GG",
      "job": "fd06aa47",
      "lock_sha": "e93a6109a019fcec2e7a25a6a906131a07e29dc0",
      "log": ".kanban/logs/304-3.log",
      "outcome": "completed",
      "pid": null,
      "pr": 331,
      "pr_head": "worktree-kb-304-3",
      "profile": "claude",
      "session_id": "fd06aa47-f118-4781-9c26-19a258ad4ad0",
      "started_at": "2026-09-03T10:58:55.833Z",
      "summary": "The GitHub store is gone; the board is the kb-board branch and a pull request is tied to its card by head branch alone. Continued attempt 1's work on origin/worktree-kb-304-1 rather than restarting, as the operator asked. PR #331 on worktree-kb-304-3, 1333 tests green, lint and tsc clean.\n\n**Attempt 1's recovered WIP was accurate, and is kept.** The 13 mid-flight wiki files in 51f13d1 read correct",
      "transcript_path": "/home/yrnd1/.claude/projects/-home-yrnd1-projects-harness-kanban-board--claude-worktrees-kb-304-3/fd06aa47-f118-4781-9c26-19a258ad4ad0.jsonl",
      "wt": "kb-304-3"
    },
    {
      "attempt": 4,
      "ended_at": "2026-09-03T11:30:39.871Z",
      "host": "YRND1-GG",
      "outcome": "changes_requested",
      "profile": "reviewer",
      "reason": "Strong card in most respects — continuing attempt 1's branch rather than restarting, verifying the\nrecovered WIP instead of trusting or discarding it, catching that this repo's own board would read as\n**empty and exit 0** (worse than the crash I predicted), and noticing that the card silently broke\n`/kanban:specify` and building the `hkb edit --body-file` verb that should have existed above `updateBody`\nall along. Those are the instincts I want.\n\nA high-effort review found 15, and **two of them invert what this card is for.** Do not treat this as a\nlong list; the first two are the card.\n\n## The two that matter\n\n1. **The card that retires GitHub made every local board read require GitHub** (forge.js:253). `fillPrs`\n   is now on the path of `hkb list`, `show`, `claim`, `context`, `graph`, `track`, `promote`, `gc --yes`\n   and the tick itself — and its catch re-throws with no offline tolerance. On `main` today a local board\n   never touches the network: `GitTier.toTask` returns `prs: []`. After this, `hkb list` on a plane, or\n   with `gh` logged out, exits non-zero with a `GhError` instead of printing the board — and the tick\n   aborts entirely. `src/init.js` still prints, in this same branch, *\"the board works with `gh` logged\n   out, on a plane, and at no API cost\"*. That sentence has to stay true: PR data is an enrichment, so a\n   forge that is unreachable must degrade to `prs: []` and a line saying so, never fail the read.\n\n2. **A plain `hkb init` over an unmigrated board silently abandons every card** (init.js:1029).\n   `resolveStore` no longer takes `existing`, so it always answers `local`; `pinStore` is then true,\n   `writeStoreKey()` rewrites a **git-tracked** `board.json` to `\"store\": \"local\"`, and `setUpLocalBoard`\n   creates an empty `kb-board` branch. The next `hkb list` reports an empty board while every real card\n   sits on GitHub. The `--import` guard twenty lines above never fires — it requires `flags.import` — and\n   `storeKind`'s \"move it with `hkb init --import`\" refusal is never reached, because `init` never calls\n   `storeKind`. The comment directly above that line still claims *\"A plain `hkb init` over an older board\n   writes nothing\"*, which is now false. **This is the command that would destroy this repository's own\n   board**, and `test/init.test.js` only covers the key being *absent*, never `store: 'github'`. Cover it.\n\n## Must fix — lock and lifecycle safety\n\n3. **The reconcile pass releases a claim out from under a live worker** (dispatch.js:297).\n   `RECONCILE_STATUSES` now includes `running`, and a merged PR is matched on **any** base branch — so a\n   reviewer merging a worker's PR mid-task (or auto-merge on a track branch) makes the next tick stamp\n   `ended_at`, `release(42, 1)`, and close the card. The worker's next heartbeat is LOCK_LOST and it exits\n   3 with no terminal verb. The old rule required GitHub to have closed the issue, which needed\n   `Closes #n` *and* a default-branch base; the new one needs neither. `entry.keep` protects the worktree,\n   not the claim.\n4. **A reopened card cannot stay reopened** (dispatch.js:244). `reconcileDecision` dropped the\n   `state === 'CLOSED'` precondition, so a card whose PR merged and which a human then reopens — via\n   `hkb request-changes`, which is what `checkOrphanedPrs`'s own fix line recommends — is forced back to\n   `done` and closed again by the very next tick, for as long as that PR stays in the newest 100 closed.\n   Same for `hkb unblock` and `hkb adopt`.\n5. **`hkb merge` now leaks the worktree and branch** (lifecycle.js:437). `mergeCard` sets `done` itself,\n   which takes the card out of `RECONCILE_STATUSES`; `sweepFinished` is driven only by\n   `summary.reconciled` (dispatch.js:621), so it never runs for a merged card. Before, the card sat in\n   `review`, the next tick reconciled it and swept immediately. Now the checkout survives until the\n   periodic full sweep — indefinitely on a board whose dispatcher is not running.\n\n## Must fix — doctor tells the truth or it is nothing\n\n6. **`checkOrphanedPrs` reports `ok` from a read that failed** (doctor.js:741): every `getTask` throw is\n   swallowed by `catch { continue; }`, so in exactly the migration window this card creates — where\n   `getTask` throws `noBoardHere` — it prints *\"N open PRs, all on cards still open\"*, a positive verdict\n   derived from nothing. The same swallow hides an offline or auth failure.\n7. **`checkClaimLock` creates the index it is diagnosing** (doctor.js:284) through the lazy `s.index`\n   getter — the exact trap this PR's own `importGithubBoard` comment names (\"a diagnosis does not create\n   what it describes\") and that `checkLocalStore` avoids with `indexFileIn`. Also: `BEGIN IMMEDIATE` and\n   `ROLLBACK` share one `try`, so a throwing ROLLBACK is reported as \"the index would not give this\n   process the write lock\" (false) and leaves the write transaction open on that handle.\n8. **`checkLocalStore` emits no line at all when `kind !== 'local'`** (doctor.js:200) — it returns\n   `undefined` with no `ok`/`warn`/`bad`, so the store check silently vanishes from the report. `kind` is\n   a caller-supplied override and the seam a second driver would use. Doctor's contract is that a skipped\n   check is distinguishable from a passing one.\n\n## Also fix\n\n9. **The tick's PR cost went up, not down** (dispatch.js:295). `mergedPrsByHead` is a second unmemoized\n   listing duplicating what `prsByHead({state: 'all'})` already returns, and `gc.js:293` takes a third\n   memo slot — up to 21 REST list calls on a gc tick, where one `state=all` listing answers all three.\n   CLAUDE.md, **rewritten by this same PR**, now states the value as \"one board read per tick and one\n   pull-request listing\".\n10. **`hkb edit --body` with no value is silently dropped** (cli.js:722): `str(true)` is `null`, the\n    \"needs at least one of\" guard passes because another flag is set, and the command reports success\n    having ignored the prose. `--body-file` two lines above throws for the same shape.\n11. **`serve.js` invalidates only `prsByHead`, not the new `prsByHeadAll`** (serve.js:450 and :478), while\n    `dropCommentCaches` was updated for both. Serve and the dispatcher share `b.ctx`, so a `state:'all'`\n    read is answered from a listing taken before the write — the exact divergence the comment two lines\n    above exists to prevent.\n12. **`DEFAULT_PROFILES` still ships `heartbeat: 'auto'`** on all five profiles (board.js:77+) with\n    comments describing the deleted ref and comment modes, so every board.json `hkb init` writes from now\n    on carries a dead key with live-sounding advice. `docs/harnesses.md` already deleted it from its\n    example.\n13. **Dead imports and a docblock describing deleted code** (doctor.js:12): `classifyClaimError`,\n    `remoteName` and `API_VERSION` are imported and unused, `storeKindOf` is defined and never called, and\n    the docblock still describes the GraphQL/lock-ref probes and a `store/github.js` import that no longer\n    exists.\n14. **`docs/harnesses.md:16` is mangled** — it duplicates its own clause and still names the lock ref as\n    step one: *\"claim the lock ref, work in the worktree, open a draft PR that / opens a draft PR on the\n    card's own branch\"*.\n15. **`docs/harnesses.md:417` still lists `KB_LOCK_REF`** among the variables the dispatcher exports,\n    which this same diff removed (`test/dispatch.test.js:975` asserts it is undefined).\n\n## Done when\n\n- [ ] Items 1 and 2 fixed, each with a test that fails against this branch. Item 2's test must be a plain\n      `hkb init` over `{store: 'github'}` asserting the key and the cards survive.\n- [ ] Items 3-5 fixed with a test each: a live worker keeps its claim through a merge; a reopened card\n      stays reopened; `hkb merge` sweeps.\n- [ ] Every other item fixed or argued in one line.\n- [ ] `npm run lint && npm test` green.\n\n## Note\n\nI am holding this PR regardless of the fix round: merging it makes this repository's board unreadable\nuntil it is migrated, and that decision is the human operator's and is pending. Item 2 is why that matters\nmore than it did an hour ago — the obvious recovery command is currently the one that destroys the board.\n",
      "started_at": "2026-09-03T11:30:39.864Z",
      "synthetic": true
    },
    {
      "attempt": 5,
      "bg": true,
      "continues_branch": "worktree-kb-304-3",
      "continues_pr": 331,
      "ended_at": "2026-09-03T11:48:56.553Z",
      "heartbeat_at": "2026-09-03T11:31:40.497Z",
      "host": "YRND1-GG",
      "job": "5cc38843",
      "lock_sha": "e93a6109a019fcec2e7a25a6a906131a07e29dc0",
      "log": ".kanban/logs/304-5.log",
      "outcome": "completed",
      "pid": null,
      "pr": 331,
      "pr_head": "worktree-kb-304-3",
      "profile": "claude",
      "session_id": "5cc38843-9ee1-41ca-8c3b-322a1601302f",
      "started_at": "2026-09-03T11:31:40.497Z",
      "summary": "All 15 review items fixed, each of the six the reviewer required a test for pinned by one that fails when the fix is mutated back. 1344 tests green (11 new), lint and tsc clean, PR #331 updated on worktree-kb-304-3 (457f943). No new PR, no force push, origin/main already merged.\n\n**The two that inverted the card.**\n\n1. *The card that retires GitHub had made every local board read require GitHub.* ",
      "transcript_path": "/home/yrnd1/.claude/projects/-home-yrnd1-projects-harness-kanban-board--claude-worktrees-kb-304-5/5cc38843-9ee1-41ca-8c3b-322a1601302f.jsonl",
      "wt": "kb-304-5"
    },
    {
      "attempt": 6,
      "ended_at": "2026-09-03T11:50:33.911Z",
      "host": "YRND1-GG",
      "outcome": "changes_requested",
      "profile": "reviewer",
      "reason": "Round 3, and it is **one item**. The other 14 are genuinely fixed and two are better than asked:\n`fillPrs`'s `required` flag is the right shape — a read degrades to `prs: []` and records\n`prsUnavailable`, while `hkb merge`, which actually needs the PR, still throws — and routing the\n`store: \"github\"` refusal through `storeKind` rather than writing init's own wording is the correct\ninstinct about where a decision lives.\n\n**Item 2 is closed for the board that declares the old store, and still open for the board that does not\ndeclare anything — which is the shape that actually exists.**\n\n`resolveStore(flags, existing)` guards on `existing?.store !== undefined && existing.store !== 'local'`.\nThis repository's `.kanban/board.json` has **no `store` key at all** — that is why the empty-read bug you\nfound in attempt 3 existed. Trace a plain `hkb init` here on this branch:\n\n- `existing.store` is `undefined`, so the new guard does not fire.\n- `asked` is `undefined`, so `resolveStore` returns `'local'`.\n- `pinStore` is false (no `--store`, no `--import`, `existing` truthy, `existing.store` undefined), so the\n  key is not written — the board stays unkeyed, which still resolves to local.\n- `setUpLocalBoard` creates an empty `kb-board` branch.\n\nResult: an empty local board over a real one. 188 cards — 57 open — sit untouched and unreachable on the\nforge, `hkb list` prints an empty board, and nothing said anything. That is the same \"silent data\nabandonment\" your own comment at init.js:655 names, reached by the one path the guard does not cover.\n\nIt is not hypothetical: it is this repository, and `hkb init` is the command an operator reaches for when\n`hkb list` starts saying \"no board here\".\n\n## What\n\nA board that is unkeyed **and** has cards on the forge is an unmigrated board, and `hkb init` must say so\nrather than create an empty one beside it.\n\n1. Before `setUpLocalBoard` creates the branch, when there is an `existing` config, no `store` key, no\n   `--import` and no local branch: ask the forge whether it has `kb:board:<slug>` issues. If it does,\n   refuse with exit 2 — name the count, and name `hkb init --import` as the way across.\n2. Offline or `gh` logged out must not turn that refusal into a hang or a crash: if the probe cannot run,\n   say what could not be checked and refuse anyway, since creating the branch is the irreversible half.\n   `--force` is the escape hatch for someone who means it.\n3. `--no-labels` (the documented offline path) must still work on a genuinely fresh repository — a repo\n   with no issues at all is not an unmigrated board.\n\n## Done when\n\n- [ ] A repo with `kb:board:default` issues and an unkeyed board.json refuses a plain `hkb init`, naming\n      the count and `--import`.\n- [ ] A fresh repo with no kb issues still inits clean.\n- [ ] The forge being unreachable refuses with a different, accurate sentence rather than proceeding.\n- [ ] `--force` proceeds, and says what it is abandoning.\n- [ ] Tests for all four. `npm run lint && npm test` green.\n\n## Still holding\n\nI am not merging this when it goes green — that is the human operator's call and it is pending, because\nmerging makes this board unreadable until it is migrated. This item is why the hold got stricter: until it\nis fixed, the recovery command an operator would reach for is itself the one that abandons the board.\n",
      "started_at": "2026-09-03T11:50:33.896Z",
      "synthetic": true
    },
    {
      "attempt": 7,
      "bg": true,
      "continues_branch": "worktree-kb-304-3",
      "continues_pr": 331,
      "ended_at": "2026-09-03T11:58:29.918Z",
      "heartbeat_at": "2026-09-03T11:51:48.786Z",
      "host": "YRND1-GG",
      "job": "a2049b42",
      "lock_sha": "e93a6109a019fcec2e7a25a6a906131a07e29dc0",
      "log": ".kanban/logs/304-7.log",
      "outcome": "completed",
      "pid": null,
      "pr": 331,
      "pr_head": "worktree-kb-304-3",
      "profile": "claude",
      "session_id": "a2049b42-82e3-4422-88be-238fc7a82165",
      "started_at": "2026-09-03T11:51:48.786Z",
      "summary": "Round 3's one item is closed: `hkb init` now refuses to create a local board over an unmigrated one that declares nothing.\n\n**The shape the guard missed.** `resolveStore(flags, existing)` guards on `existing?.store !== undefined && existing.store !== 'local'`, which answers for a board.json that *says* `\"store\": \"github\"`. This repository's board.json — and every one written before the key existed",
      "transcript_path": "/home/yrnd1/.claude/projects/-home-yrnd1-projects-harness-kanban-board--claude-worktrees-kb-304-7/a2049b42-82e3-4422-88be-238fc7a82165.jsonl",
      "wt": "kb-304-7"
    },
    {
      "attempt": 8,
      "ended_at": "2026-09-03T15:22:18.594Z",
      "host": "YRND1-GG",
      "outcome": "changes_requested",
      "profile": "reviewer",
      "reason": "Round 4, and it is **one item folded in from #330** — everything else is done. Round 3's refusal is\nverified: `refuseUnmigratedBoard` covers the unkeyed board, the unreachable forge and `--force`, and you\nmutation-tested it (`if (false) await refuseUnmigratedBoard(...)` fails the three new tests while \"a repo\nwith no kb cards inits clean\" correctly still passes). That is the bar.\n\n**Why this is landing here instead of on #330.** #330 was blocked on this card, and that ordering could\nnever execute: merging this card makes the operator's board unreadable until it is migrated, so the\ndispatcher cannot dispatch #330 afterwards, and running it before means fixing a file this card deletes.\nThe migration path this card keeps alive in `src/init.js` is the only place the fix belongs. So it comes\nin here, and #330 closes as folded when this merges.\n\n**The operator has approved migrating this repository's board immediately after this merges.** That makes\nthe import's fidelity the last thing standing between the plan and a real board, and it is why this is\nworth one more round rather than a follow-up.\n\n## The item\n\n`importGithubBoard` reads closed cards through `listClosedRecent({first: CLOSED_PAGE})`, and\n`fetchClosedRecent` is one query with no `pageInfo` loop — its own comment says *\"One query, no paging\"*.\nRound 3 of #294 made the cap honest (`closed_capped`, `closed_page` in the summary) but not absent.\n\n**Measured on the board that is about to be migrated:**\n\n| | |\n|---|---|\n| kb cards total | 188 |\n| open | 57 |\n| closed | 131 |\n| closed inside the 90-day window | 131 |\n| dropped by the window | **0** |\n| dropped by the single-page cap | **31** |\n\nSo 31 closed cards — their run records, results and handoffs — would not migrate. The issues stay on\nGitHub, but the local board becomes the source of truth without them, and `next_id` is computed from the\ntruncated set.\n\n1. Paginate `fetchClosedRecent` on `pageInfo.hasNextPage`/`endCursor` until the **window** is exhausted,\n   not until a page is full. Keep the 90-day filter: that is a deliberate scope, where the page size never\n   was.\n2. `closed_capped` should mean \"a real ceiling was hit\", never \"the first page ran out\".\n3. `next_id` computed after the full read.\n4. Same shape in `adoptOpenIssues`: #294's review found `more = page === pages` reports a false positive\n   on an exact multiple of the page size. Fix it while you are here — a warning that cries wolf trains the\n   reader to ignore it.\n5. The probe you added in round 3 has the same one-page limit and says so in its own residual note (\"at\n   least 100\"). That one is fine as is — it only needs a yes/no — but say so in a comment so the next\n   reader does not \"fix\" it into a paginated call on a hot path.\n\n## Done when\n\n- [ ] A fixture board with 250 cards closed inside the window imports all 250, and `closed_capped` is\n      false.\n- [ ] `closed_capped` is true only when a real ceiling was hit, with a test for that case.\n- [ ] `adoptOpenIssues`'s `more` is false on an exact multiple of the page size.\n- [ ] Each test verified to fail against the pre-fix code — say so, as you did last round.\n- [ ] `npm run lint && npm test` green.\n\n## What happens next, so you know what this is for\n\nThe operator merges this, runs `hkb init --import` on this repository immediately, verifies all 188 cards\nlanded on the `kb-board` branch, and restarts the loop on the local store. If your pagination is wrong,\n31 cards of this project's own history quietly do not make it. Please test it against a fixture larger\nthan one page rather than assuming the loop terminates.\n",
      "started_at": "2026-09-03T15:22:18.580Z",
      "synthetic": true
    },
    {
      "attempt": 9,
      "bg": true,
      "continues_branch": "worktree-kb-304-3",
      "continues_pr": 331,
      "ended_at": "2026-09-03T15:45:10.926Z",
      "heartbeat_at": "2026-09-03T15:35:07.992Z",
      "host": "YRND1-GG",
      "job": "918c8096",
      "lock_sha": "238a3b65d997c2eb59d8794d893af1d8c3252037",
      "log": ".kanban/logs/304-9.log",
      "outcome": "completed",
      "pid": null,
      "pr": 331,
      "pr_head": "worktree-kb-304-3",
      "profile": "claude",
      "session_id": "918c8096-78d0-441a-9009-b2550694d815",
      "started_at": "2026-09-03T15:35:07.992Z",
      "summary": "Round 4's one item is closed: the migration's closed-card read pages to its window instead of to a page, and `closed_capped` now means a ceiling that was really reached.\n\n**What was wrong.** `fetchClosedRecent` was one GraphQL query — its own comment said \"One query, no paging\" — so the page size was the ceiling by accident. On the board about to be migrated that is 131 cards closed inside the 90-",
      "transcript_path": "/home/yrnd1/.claude/projects/-home-yrnd1-projects-harness-kanban-board--claude-worktrees-kb-304-9/918c8096-78d0-441a-9009-b2550694d815.jsonl",
      "wt": "kb-304-9"
    },
    {
      "attempt": 10,
      "ended_at": "2026-09-03T22:55:53.337Z",
      "host": "YRND1-GG",
      "outcome": "changes_requested",
      "profile": "reviewer",
      "reason": "Round 5, and it is **not a review** — the four rounds are done and this PR is green and correct on its own\nterms. #334 landed on `main` while you were in review (`3e17acd`, \"Move the board off refs/heads\"), and\nthis branch predates it. `git log pr331..origin/main` shows exactly that one commit missing, and it changes\nthe same files you do, so merging as-is would silently revert it.\n\n**Merge `origin/main` into this branch, resolve, and make sure both halves survive.**\n\n## What #334 changed, all of which must still be true afterwards\n\nThe durable tier moved off `refs/heads/kb-board` to `refs/kb/boards/<slug>` — invisible to `git branch` and\nto GitHub's UI, which was the point.\n\n- `boardRef(slug)` / `isBoardSlug(slug)` in `src/store/git.js` — one place spells the ref; `SLUG_RE` rejects\n  a trailing dot, `.lock` and `..`, because `git check-ref-format` does.\n- `boardFetchRefspec(remote)` maps to **`refs/kb/remotes/<remote>/boards/*`** — deliberately *not* under\n  `refs/remotes/`, where a remote branch named `kb` makes it a directory/file conflict that breaks\n  `git fetch` for the whole repository. The docblock carries the measured error; do not \"simplify\" it back.\n- `isBoardFetchRefspec` matches the **destination**, and `ensureFetchRefspec`/`hasFetchRefspec` share it.\n  Matching the source let `+refs/kb/*:refs/kb/*` read as present, and under that config a plain\n  `git fetch` force-rewinds the local board to the remote's older sha.\n- `update-ref --create-reflog` in `_land` — a ref outside `refs/heads` gets no reflog by default, so the\n  durable tier had lost its recovery record.\n- `localBoardExists` also probes `refs/heads/kb-board`, so a board on the old ref is *named* rather than\n  silently replaced by a new empty one.\n\n## What this card changed, which must equally survive\n\nThe GitHub store is gone; `refuseUnmigratedBoard` (the unkeyed-board, unreachable-forge and `--force`\ncases); `fillPrs`'s `required` flag so a read degrades offline while `hkb merge` still throws; and the\nimport's closed-card read paginating to its window rather than to one page.\n\n## Two places they will actually collide\n\n1. **The board-missing sentence.** You added `noBoardHere` naming all three ways across\n   (`hkb init`, `hkb init --import`, the fetch); #334 rewrote the tier's own refusals to \"there is no board\n   at `<ref>` in `<root>`\". Pick one voice and use it everywhere — the review of #334 already flagged that\n   card for leaving two vocabularies behind, so do not land a third.\n2. **`localBoardExists`.** Both cards edited it, for different reasons: yours resolves the store, #334's\n   added the old-ref probe and fixed the remote-name derivation. Keep both properties.\n\n## Done when\n\n- [ ] `git merge origin/main` resolved, no force-push.\n- [ ] `git branch -a` on a scratch board still shows no board branch, and `refs/kb/boards/default` exists.\n- [ ] `hkb init` still refuses an unkeyed board that has cards on the forge.\n- [ ] Both test suites green together: `npm run lint && npm test`.\n- [ ] Say in the result which conflicts you resolved and which side won each — that is the part I will\n      check.\n\n## Why the hurry, so you know what this is holding\n\nThe operator merges this the moment it is green, then immediately migrates this repository's own board off\nGitHub with `hkb init --import` — 188 cards, 57 open. Everything after it (TypeScript, root C) is waiting\non that. If the merge quietly loses #334's ref change, the board gets created on a branch that was just\nremoved, on a board with no going back.\n",
      "started_at": "2026-09-03T22:55:53.325Z",
      "synthetic": true
    },
    {
      "attempt": 11,
      "bg": true,
      "continues_branch": "worktree-kb-304-3",
      "continues_pr": 331,
      "ended_at": "2026-09-04T03:54:54.184Z",
      "heartbeat_at": "2026-09-04T03:39:26.692Z",
      "host": "YRND1-GG",
      "job": "6df701ca",
      "lock_sha": "3e17acde07fc55b807eb93423bb0092678d4c37d",
      "log": ".kanban/logs/304-11.log",
      "merged_by": "operator",
      "outcome": "completed",
      "pid": null,
      "pr": 331,
      "pr_head": "worktree-kb-304-3",
      "profile": "claude",
      "session_id": "6df701ca-2181-4efe-841b-7d0c717b859b",
      "started_at": "2026-09-04T03:39:26.692Z",
      "summary": "Round 5 was the merge, not a review round. `origin/main` (3e17acd, #334 — the board moved off `refs/heads/kb-board` to `refs/kb/boards/<slug>`) is merged into `worktree-kb-304-3` and pushed to PR #331 as e12e570. No rebase, no force. 1366 tests green, lint and tsc clean, `git log pr331..origin/main` is now empty.\n\n**Both halves verified on a scratch board, not just in tests.** `hkb init` there: `g",
      "transcript_path": "/home/yrnd1/.claude/projects/-home-yrnd1-projects-harness-kanban-board--claude-worktrees-kb-304-11/6df701ca-2181-4efe-841b-7d0c717b859b.jsonl",
      "wt": "kb-304-11"
    }
  ],
  "block_loops": {
    "t9s46d": 1
  },
  "failures": 0,
  "last_error": "reclaimed after 3615s",
  "notes": [
    {
      "actor": "emyann",
      "at": "2026-09-03T10:56:58Z",
      "id": 5524652555,
      "text": "**Attempt 1's work is preserved — do not start over.**\n\nAttempt 1 ran for about an hour and was then reclaimed, not because it failed or timed out\n(`max_runtime` is 3 h) but because its Claude session daemon went away: `claude logs` answers\n`connect ENOENT /tmp/cc-daemon-1000/…/control.sock`. It stopped heartbeating, and the tick did the right\nthing. Nothing about the card caused it.\n\nIts work is on **`origin/worktree-kb-304-1`**, pushed by the operator:\n\n- `e5c4da2` — the branch name is the only link: the track brief too, and `kb/track-<n>` resolves to its root\n- `da32e16` — the skill and the protocol reference: two tiers, a claim row, and the branch as the link\n- `e16f166` — README, CLAUDE.md, the doc section and the by-hand guide: the board is a branch, the branch\n  name is the link\n- `51f13d1` — **WIP recovered by the operator**: 13 wiki files that were modified but never committed\n  (+370/−290). These are mid-flight and unverified. Read them, finish or replace them, and squash this\n  commit away. Do not assume they are correct.\n\nSo: `git fetch origin worktree-kb-304-1` and continue on that branch rather than re-doing the docs half.\nJudge the recovered WIP on its merits — if it is wrong, throwing it out is a fine outcome and worth one\nline saying so.\n\n---\n\n**One thing this card must answer, because it is what makes it un-mergeable otherwise.**\n\nThis repository's own board has **no `store` key** in `.kanban/board.json`, so `storeKind` resolves it to\n`github`. This card deletes `src/store/github.js`. The moment it merges, the dispatcher driving this board\ncannot read it, and neither can any worker — their worktrees come from `main`. The card's own contract says\nnot to migrate this repository's board (root C does that, after), which leaves a window where `main` cannot\ndrive the board it lives on.\n\nDo **not** migrate this board to resolve that — that is the operator's call and it is pending. What this\ncard needs to do instead is make the failure *legible*: when `storeKind` resolves to a store that no longer\nexists, say so in words that name `hkb init --import` as the way across, with an `.exitCode`, rather than\nthrowing on a missing module. A board left on the retired store must get a sentence, not a stack trace.\n\nAlso note #330 (blocked on this card) fixes the import's single-page cap — measured on this board, 31 of\n131 closed cards would silently not migrate today. Your `grep` criterion already excludes `src/init.js`'s\nimport path from the deletion, so leave that path readable enough for #330 to fix."
    },
    {
      "actor": "emyann",
      "at": "2026-09-03T10:58:05Z",
      "id": 5524665178,
      "text": "**Blocked** (transient, attempt 2): Operator: restarting so the recovery note above reaches the brief — it posted 47s after this attempt claimed, so attempt 2 never saw it and would have redone attempt 1's hour of work from scratch. Nothing wrong with the attempt."
    },
    {
      "actor": "emyann",
      "at": "2026-09-03T11:30:39Z",
      "id": 5525041329,
      "text": "**Changes requested** (after attempt 3): Strong card in most respects — continuing attempt 1's branch rather than restarting, verifying the\nrecovered WIP instead of trusting or discarding it, catching that this repo's own board would read as\n**empty and exit 0** (worse than the crash I predicted), and noticing that the card silently broke\n`/kanban:specify` and building the `hkb edit --body-file` verb that should have existed above `updateBody`\nall along. Those are the instincts I want.\n\nA high-effort review found 15, and **two of them invert what this card is for.** Do not treat this as a\nlong list; the first two are the card.\n\n## The two that matter\n\n1. **The card that retires GitHub made every local board read require GitHub** (forge.js:253). `fillPrs`\n   is now on the path of `hkb list`, `show`, `claim`, `context`, `graph`, `track`, `promote`, `gc --yes`\n   and the tick itself — and its catch re-throws with no offline tolerance. On `main` today a local board\n   never touches the network: `GitTier.toTask` returns `prs: []`. After this, `hkb list` on a plane, or\n   with `gh` logged out, exits non-zero with a `GhError` instead of printing the board — and the tick\n   aborts entirely. `src/init.js` still prints, in this same branch, *\"the board works with `gh` logged\n   out, on a plane, and at no API cost\"*. That sentence has to stay true: PR data is an enrichment, so a\n   forge that is unreachable must degrade to `prs: []` and a line saying so, never fail the read.\n\n2. **A plain `hkb init` over an unmigrated board silently abandons every card** (init.js:1029).\n   `resolveStore` no longer takes `existing`, so it always answers `local`; `pinStore` is then true,\n   `writeStoreKey()` rewrites a **git-tracked** `board.json` to `\"store\": \"local\"`, and `setUpLocalBoard`\n   creates an empty `kb-board` branch. The next `hkb list` reports an empty board while every real card\n   sits on GitHub. The `--import` guard twenty lines above never fires — it requires `flags.import` — and\n   `storeKind`'s \"move it with `hkb init --import`\" refusal is never reached, because `init` never calls\n   `storeKind`. The comment directly above that line still claims *\"A plain `hkb init` over an older board\n   writes nothing\"*, which is now false. **This is the command that would destroy this repository's own\n   board**, and `test/init.test.js` only covers the key being *absent*, never `store: 'github'`. Cover it.\n\n## Must fix — lock and lifecycle safety\n\n3. **The reconcile pass releases a claim out from under a live worker** (dispatch.js:297).\n   `RECONCILE_STATUSES` now includes `running`, and a merged PR is matched on **any** base branch — so a\n   reviewer merging a worker's PR mid-task (or auto-merge on a track branch) makes the next tick stamp\n   `ended_at`, `release(42, 1)`, and close the card. The worker's next heartbeat is LOCK_LOST and it exits\n   3 with no terminal verb. The old rule required GitHub to have closed the issue, which needed\n   `Closes #n` *and* a default-branch base; the new one needs neither. `entry.keep` protects the worktree,\n   not the claim.\n4. **A reopened card cannot stay reopened** (dispatch.js:244). `reconcileDecision` dropped the\n   `state === 'CLOSED'` precondition, so a card whose PR merged and which a human then reopens — via\n   `hkb request-changes`, which is what `checkOrphanedPrs`'s own fix line recommends — is forced back to\n   `done` and closed again by the very next tick, for as long as that PR stays in the newest 100 closed.\n   Same for `hkb unblock` and `hkb adopt`.\n5. **`hkb merge` now leaks the worktree and branch** (lifecycle.js:437). `mergeCard` sets `done` itself,\n   which takes the card out of `RECONCILE_STATUSES`; `sweepFinished` is driven only by\n   `summary.reconciled` (dispatch.js:621), so it never runs for a merged card. Before, the card sat in\n   `review`, the next tick reconciled it and swept immediately. Now the checkout survives until the\n   periodic full sweep — indefinitely on a board whose dispatcher is not running.\n\n## Must fix — doctor tells the truth or it is nothing\n\n6. **`checkOrphanedPrs` reports `ok` from a read that failed** (doctor.js:741): every `getTask` throw is\n   swallowed by `catch { continue; }`, so in exactly the migration window this card creates — where\n   `getTask` throws `noBoardHere` — it prints *\"N open PRs, all on cards still open\"*, a positive verdict\n   derived from nothing. The same swallow hides an offline or auth failure.\n7. **`checkClaimLock` creates the index it is diagnosing** (doctor.js:284) through the lazy `s.index`\n   getter — the exact trap this PR's own `importGithubBoard` comment names (\"a diagnosis does not create\n   what it describes\") and that `checkLocalStore` avoids with `indexFileIn`. Also: `BEGIN IMMEDIATE` and\n   `ROLLBACK` share one `try`, so a throwing ROLLBACK is reported as \"the index would not give this\n   process the write lock\" (false) and leaves the write transaction open on that handle.\n8. **`checkLocalStore` emits no line at all when `kind !== 'local'`** (doctor.js:200) — it returns\n   `undefined` with no `ok`/`warn`/`bad`, so the store check silently vanishes from the report. `kind` is\n   a caller-supplied override and the seam a second driver would use. Doctor's contract is that a skipped\n   check is distinguishable from a passing one.\n\n## Also fix\n\n9. **The tick's PR cost went up, not down** (dispatch.js:295). `mergedPrsByHead` is a second unmemoized\n   listing duplicating what `prsByHead({state: 'all'})` already returns, and `gc.js:293` takes a third\n   memo slot — up to 21 REST list calls on a gc tick, where one `state=all` listing answers all three.\n   CLAUDE.md, **rewritten by this same PR**, now states the value as \"one board read per tick and one\n   pull-request listing\".\n10. **`hkb edit --body` with no value is silently dropped** (cli.js:722): `str(true)` is `null`, the\n    \"needs at least one of\" guard passes because another flag is set, and the command reports success\n    having ignored the prose. `--body-file` two lines above throws for the same shape.\n11. **`serve.js` invalidates only `prsByHead`, not the new `prsByHeadAll`** (serve.js:450 and :478), while\n    `dropCommentCaches` was updated for both. Serve and the dispatcher share `b.ctx`, so a `state:'all'`\n    read is answered from a listing taken before the write — the exact divergence the comment two lines\n    above exists to prevent.\n12. **`DEFAULT_PROFILES` still ships `heartbeat: 'auto'`** on all five profiles (board.js:77+) with\n    comments describing the deleted ref and comment modes, so every board.json `hkb init` writes from now\n    on carries a dead key with live-sounding advice. `docs/harnesses.md` already deleted it from its\n    example.\n13. **Dead imports and a docblock describing deleted code** (doctor.js:12): `classifyClaimError`,\n    `remoteName` and `API_VERSION` are imported and unused, `storeKindOf` is defined and never called, and\n    the docblock still describes the GraphQL/lock-ref probes and a `store/github.js` import that no longer\n    exists.\n14. **`docs/harnesses.md:16` is mangled** — it duplicates its own clause and still names the lock ref as\n    step one: *\"claim the lock ref, work in the worktree, open a draft PR that / opens a draft PR on the\n    card's own branch\"*.\n15. **`docs/harnesses.md:417` still lists `KB_LOCK_REF`** among the variables the dispatcher exports,\n    which this same diff removed (`test/dispatch.test.js:975` asserts it is undefined).\n\n## Done when\n\n- [ ] Items 1 and 2 fixed, each with a test that fails against this branch. Item 2's test must be a plain\n      `hkb init` over `{store: 'github'}` asserting the key and the cards survive.\n- [ ] Items 3-5 fixed with a test each: a live worker keeps its claim through a merge; a reopened card\n      stays reopened; `hkb merge` sweeps.\n- [ ] Every other item fixed or argued in one line.\n- [ ] `npm run lint && npm test` green.\n\n## Note\n\nI am holding this PR regardless of the fix round: merging it makes this repository's board unreadable\nuntil it is migrated, and that decision is the human operator's and is pending. Item 2 is why that matters\nmore than it did an hour ago — the obvious recovery command is currently the one that destroys the board.\n"
    },
    {
      "actor": "emyann",
      "at": "2026-09-03T11:50:34Z",
      "id": 5525279341,
      "text": "**Changes requested** (after attempt 5): Round 3, and it is **one item**. The other 14 are genuinely fixed and two are better than asked:\n`fillPrs`'s `required` flag is the right shape — a read degrades to `prs: []` and records\n`prsUnavailable`, while `hkb merge`, which actually needs the PR, still throws — and routing the\n`store: \"github\"` refusal through `storeKind` rather than writing init's own wording is the correct\ninstinct about where a decision lives.\n\n**Item 2 is closed for the board that declares the old store, and still open for the board that does not\ndeclare anything — which is the shape that actually exists.**\n\n`resolveStore(flags, existing)` guards on `existing?.store !== undefined && existing.store !== 'local'`.\nThis repository's `.kanban/board.json` has **no `store` key at all** — that is why the empty-read bug you\nfound in attempt 3 existed. Trace a plain `hkb init` here on this branch:\n\n- `existing.store` is `undefined`, so the new guard does not fire.\n- `asked` is `undefined`, so `resolveStore` returns `'local'`.\n- `pinStore` is false (no `--store`, no `--import`, `existing` truthy, `existing.store` undefined), so the\n  key is not written — the board stays unkeyed, which still resolves to local.\n- `setUpLocalBoard` creates an empty `kb-board` branch.\n\nResult: an empty local board over a real one. 188 cards — 57 open — sit untouched and unreachable on the\nforge, `hkb list` prints an empty board, and nothing said anything. That is the same \"silent data\nabandonment\" your own comment at init.js:655 names, reached by the one path the guard does not cover.\n\nIt is not hypothetical: it is this repository, and `hkb init` is the command an operator reaches for when\n`hkb list` starts saying \"no board here\".\n\n## What\n\nA board that is unkeyed **and** has cards on the forge is an unmigrated board, and `hkb init` must say so\nrather than create an empty one beside it.\n\n1. Before `setUpLocalBoard` creates the branch, when there is an `existing` config, no `store` key, no\n   `--import` and no local branch: ask the forge whether it has `kb:board:<slug>` issues. If it does,\n   refuse with exit 2 — name the count, and name `hkb init --import` as the way across.\n2. Offline or `gh` logged out must not turn that refusal into a hang or a crash: if the probe cannot run,\n   say what could not be checked and refuse anyway, since creating the branch is the irreversible half.\n   `--force` is the escape hatch for someone who means it.\n3. `--no-labels` (the documented offline path) must still work on a genuinely fresh repository — a repo\n   with no issues at all is not an unmigrated board.\n\n## Done when\n\n- [ ] A repo with `kb:board:default` issues and an unkeyed board.json refuses a plain `hkb init`, naming\n      the count and `--import`.\n- [ ] A fresh repo with no kb issues still inits clean.\n- [ ] The forge being unreachable refuses with a different, accurate sentence rather than proceeding.\n- [ ] `--force` proceeds, and says what it is abandoning.\n- [ ] Tests for all four. `npm run lint && npm test` green.\n\n## Still holding\n\nI am not merging this when it goes green — that is the human operator's call and it is pending, because\nmerging makes this board unreadable until it is migrated. This item is why the hold got stricter: until it\nis fixed, the recovery command an operator would reach for is itself the one that abandons the board.\n"
    },
    {
      "actor": "emyann",
      "at": "2026-09-03T15:22:20Z",
      "id": 5527963880,
      "text": "**Changes requested** (after attempt 7): Round 4, and it is **one item folded in from #330** — everything else is done. Round 3's refusal is\nverified: `refuseUnmigratedBoard` covers the unkeyed board, the unreachable forge and `--force`, and you\nmutation-tested it (`if (false) await refuseUnmigratedBoard(...)` fails the three new tests while \"a repo\nwith no kb cards inits clean\" correctly still passes). That is the bar.\n\n**Why this is landing here instead of on #330.** #330 was blocked on this card, and that ordering could\nnever execute: merging this card makes the operator's board unreadable until it is migrated, so the\ndispatcher cannot dispatch #330 afterwards, and running it before means fixing a file this card deletes.\nThe migration path this card keeps alive in `src/init.js` is the only place the fix belongs. So it comes\nin here, and #330 closes as folded when this merges.\n\n**The operator has approved migrating this repository's board immediately after this merges.** That makes\nthe import's fidelity the last thing standing between the plan and a real board, and it is why this is\nworth one more round rather than a follow-up.\n\n## The item\n\n`importGithubBoard` reads closed cards through `listClosedRecent({first: CLOSED_PAGE})`, and\n`fetchClosedRecent` is one query with no `pageInfo` loop — its own comment says *\"One query, no paging\"*.\nRound 3 of #294 made the cap honest (`closed_capped`, `closed_page` in the summary) but not absent.\n\n**Measured on the board that is about to be migrated:**\n\n| | |\n|---|---|\n| kb cards total | 188 |\n| open | 57 |\n| closed | 131 |\n| closed inside the 90-day window | 131 |\n| dropped by the window | **0** |\n| dropped by the single-page cap | **31** |\n\nSo 31 closed cards — their run records, results and handoffs — would not migrate. The issues stay on\nGitHub, but the local board becomes the source of truth without them, and `next_id` is computed from the\ntruncated set.\n\n1. Paginate `fetchClosedRecent` on `pageInfo.hasNextPage`/`endCursor` until the **window** is exhausted,\n   not until a page is full. Keep the 90-day filter: that is a deliberate scope, where the page size never\n   was.\n2. `closed_capped` should mean \"a real ceiling was hit\", never \"the first page ran out\".\n3. `next_id` computed after the full read.\n4. Same shape in `adoptOpenIssues`: #294's review found `more = page === pages` reports a false positive\n   on an exact multiple of the page size. Fix it while you are here — a warning that cries wolf trains the\n   reader to ignore it.\n5. The probe you added in round 3 has the same one-page limit and says so in its own residual note (\"at\n   least 100\"). That one is fine as is — it only needs a yes/no — but say so in a comment so the next\n   reader does not \"fix\" it into a paginated call on a hot path.\n\n## Done when\n\n- [ ] A fixture board with 250 cards closed inside the window imports all 250, and `closed_capped` is\n      false.\n- [ ] `closed_capped` is true only when a real ceiling was hit, with a test for that case.\n- [ ] `adoptOpenIssues`'s `more` is false on an exact multiple of the page size.\n- [ ] Each test verified to fail against the pre-fix code — say so, as you did last round.\n- [ ] `npm run lint && npm test` green.\n\n## What happens next, so you know what this is for\n\nThe operator merges this, runs `hkb init --import` on this repository immediately, verifies all 188 cards\nlanded on the `kb-board` branch, and restarts the loop on the local store. If your pagination is wrong,\n31 cards of this project's own history quietly do not make it. Please test it against a fixture larger\nthan one page rather than assuming the loop terminates.\n"
    },
    {
      "actor": "emyann",
      "at": "2026-09-03T22:55:55Z",
      "id": 5533184333,
      "text": "**Changes requested** (after attempt 9): Round 5, and it is **not a review** — the four rounds are done and this PR is green and correct on its own\nterms. #334 landed on `main` while you were in review (`3e17acd`, \"Move the board off refs/heads\"), and\nthis branch predates it. `git log pr331..origin/main` shows exactly that one commit missing, and it changes\nthe same files you do, so merging as-is would silently revert it.\n\n**Merge `origin/main` into this branch, resolve, and make sure both halves survive.**\n\n## What #334 changed, all of which must still be true afterwards\n\nThe durable tier moved off `refs/heads/kb-board` to `refs/kb/boards/<slug>` — invisible to `git branch` and\nto GitHub's UI, which was the point.\n\n- `boardRef(slug)` / `isBoardSlug(slug)` in `src/store/git.js` — one place spells the ref; `SLUG_RE` rejects\n  a trailing dot, `.lock` and `..`, because `git check-ref-format` does.\n- `boardFetchRefspec(remote)` maps to **`refs/kb/remotes/<remote>/boards/*`** — deliberately *not* under\n  `refs/remotes/`, where a remote branch named `kb` makes it a directory/file conflict that breaks\n  `git fetch` for the whole repository. The docblock carries the measured error; do not \"simplify\" it back.\n- `isBoardFetchRefspec` matches the **destination**, and `ensureFetchRefspec`/`hasFetchRefspec` share it.\n  Matching the source let `+refs/kb/*:refs/kb/*` read as present, and under that config a plain\n  `git fetch` force-rewinds the local board to the remote's older sha.\n- `update-ref --create-reflog` in `_land` — a ref outside `refs/heads` gets no reflog by default, so the\n  durable tier had lost its recovery record.\n- `localBoardExists` also probes `refs/heads/kb-board`, so a board on the old ref is *named* rather than\n  silently replaced by a new empty one.\n\n## What this card changed, which must equally survive\n\nThe GitHub store is gone; `refuseUnmigratedBoard` (the unkeyed-board, unreachable-forge and `--force`\ncases); `fillPrs`'s `required` flag so a read degrades offline while `hkb merge` still throws; and the\nimport's closed-card read paginating to its window rather than to one page.\n\n## Two places they will actually collide\n\n1. **The board-missing sentence.** You added `noBoardHere` naming all three ways across\n   (`hkb init`, `hkb init --import`, the fetch); #334 rewrote the tier's own refusals to \"there is no board\n   at `<ref>` in `<root>`\". Pick one voice and use it everywhere — the review of #334 already flagged that\n   card for leaving two vocabularies behind, so do not land a third.\n2. **`localBoardExists`.** Both cards edited it, for different reasons: yours resolves the store, #334's\n   added the old-ref probe and fixed the remote-name derivation. Keep both properties.\n\n## Done when\n\n- [ ] `git merge origin/main` resolved, no force-push.\n- [ ] `git branch -a` on a scratch board still shows no board branch, and `refs/kb/boards/default` exists.\n- [ ] `hkb init` still refuses an unkeyed board that has cards on the forge.\n- [ ] Both test suites green together: `npm run lint && npm test`.\n- [ ] Say in the result which conflicts you resolved and which side won each — that is the part I will\n      check.\n\n## Why the hurry, so you know what this is holding\n\nThe operator merges this the moment it is green, then immediately migrates this repository's own board off\nGitHub with `hkb init --import` — 188 cards, 57 open. Everything after it (TypeScript, root C) is waiting\non that. If the merge quietly loses #334's ref change, the board gets created on a branch that was just\nremoved, on a board with no going back.\n"
    },
    {
      "actor": "emyann",
      "at": "2026-09-04T03:56:43Z",
      "id": 5535432382,
      "text": "**Merged by the operator seat** — review: Five rounds: four of review (30+ findings, three that would have destroyed live state) and a fifth that was the merge of #334 rather than a review. Verified by hand on the merged branch that both halves survived, since a stale merge here would have silently reverted the ref change:  From #334 — the board at refs/kb/boards/<slug>; the fetch refspec into refs/kb/remotes/<remote>/boards/* (not refs/remotes, where a branch named kb breaks git fetch repo-wide); update-ref --create-reflog; isBoardFetchRefspec matching the destination; the refs/heads/kb-board probe so a board on the old ref is named rather than replaced; isBoardSlug rejecting what git check-ref-format rejects. All six present.  From this card — src/store/github.js deleted; refuseUnmigratedBoard covering the unkeyed board, the unreachable forge and --force; fillPrs required flag so a local read degrades offline while hkb merge still throws; and the migration read path paginating its window, now living in src/bridge/github-issues.js, which is ADR-006 own framing of GitHub as a bridge.  Earlier rounds caught, among others: every local board read made to require GitHub (the inverse of this card purpose); a plain hkb init silently abandoning every card of an unmigrated board; the reconcile pass releasing a claim out from under a live worker; hkb merge no longer sweeping its worktree; and doctor reporting ok from a board read that had failed.  CI green on the merge commit, 1366 tests, lint and tsc clean. The operator migrates this repository own board immediately after this lands., checks: green, method: squash"
    }
  ],
  "results": [
    {
      "artifacts": [],
      "at": "2026-09-03T11:18:22Z",
      "attempt": 3,
      "kind": "result",
      "metadata": {
        "changed_files": [
          "src/model.js",
          "src/store/git.js",
          "src/store/index.js",
          "src/store/local.js",
          "src/store/sqlite.js",
          "src/cli.js",
          "src/board.js",
          "src/doctor.js",
          "src/dispatch.js",
          "src/gc.js",
          "src/serve.js",
          "src/track.js",
          "skills/kanban/SKILL.md",
          "skills/kanban/references/protocol.md",
          "test/cli.test.js",
          "test/dispatch.test.js",
          "test/model.test.js",
          "test/store-git.test.js",
          "test/store.test.js",
          "test/fake-store.js",
          "test/up.test.js",
          "test/doctor.test.js",
          "test/init.test.js",
          "docs/wiki/architecture/overview.md",
          "docs/wiki/architecture/store-seam.md",
          "docs/wiki/architecture/local-store.md",
          "docs/wiki/architecture/kb-board-branch.md",
          "docs/wiki/concepts/store.md",
          "docs/wiki/concepts/worker-identity.md",
          "docs/wiki/features/review-loop.md",
          "docs/wiki/features/auto-merge.md",
          "docs/wiki/features/backlog-grooming.md",
          "docs/wiki/log.md",
          "docs/wiki/index.md"
        ],
        "residual_risk": [
          "THE ONE THING TO DECIDE BEFORE MERGING: this repository's own board is still on GitHub Issues and this card does not migrate it, per its contract. Merging makes the board unreadable from main until `hkb init --import` runs. That failure is now a sentence naming the migration rather than an empty list, which is what the operator asked for, but it is still a failure — and #330's fix to the import's single-page cap should land first (31 of 131 closed cards would not migrate today).",
          "listTasks/getTask now throw on a repository with no kb-board branch. Every caller in the suite is happy, but a *new* caller that reasonably expects an empty board on a fresh checkout — a `hkb doctor` check, a future read-only surface — will get an exception instead. The write path has always thrown here, so the two halves now agree; if that turns out to be wrong for one specific caller, the fix is a `{ tolerateMissing: true }` on that read, not a return to answering [].",
          "`hkb edit --body-file` is new CLI surface this card did not ask for. It is justified — the card is what broke /kanban:specify, and the alternative was leaving the skill telling operators to PATCH an issue that is not the board — but it is a verb a reviewer did not spec, and it has no `--title` (setTitle is not on the Store interface, and adding it would need a conformance scenario). A card's title still cannot be edited from the CLI.",
          "The double's setKb changed shape (bodyText now defaults to the record's prose, not the passed task's). Every existing test passes, but a test that relied on the old clobbering behaviour to reset a body would now see the body survive. The conformance scenario states the new pairing on all drivers.",
          "Attempt 1's WIP commit 51f13d1 is still in the history rather than squashed, against the operator's suggestion. Its content is verified and finished; the squash merge does not carry it into main. If the branch itself must be clean, that is a rebase and a force push by someone the never-force-push rule does not bind."
        ],
        "verification": [
          "npm run lint && npm test — 1333 tests, 1333 pass, 0 fail; tsc clean (7 new tests)",
          "grep -rn \"Closes #\" src skills — empty",
          "grep -an \"KB_LOCK_REF|HEARTBEAT_MODES\" src — empty; lock_sha survives only as an index column, with a comment saying it is dead as live state",
          "src/store/github.js, src/tasks.js, src/lock.js, src/projects.js — all absent",
          "node bin/hkb.js init --store github — refused by name, naming `hkb init --import`",
          "node bin/hkb.js list / show 304 against this repository's unmigrated board — both print the noBoardHere sentence and exit 2, where they printed \"(no tasks)\" and exited 0 before the change",
          "the new dispatch test carries its own negative cases (a card whose branch nobody merged, and a merged PR on release/2.0 belonging to no card), so it fails if the head-branch match is loosened or dropped",
          "node .repolore/scripts/wiki-check.mjs — the four pages the card names plus the four store pages and features/backlog-grooming are fresh; 19 remain stale, all of them stale before this branch or made so by broad edits to model.js/board.js/dispatch.js that do not touch their claims",
          "git fetch origin main && git merge origin/main — already up to date; pushed to worktree-kb-304-3, no force",
          "PR #331, draft, body contains Closes #304"
        ]
      },
      "pr": 331,
      "pr_continued": false,
      "summary": "The GitHub store is gone; the board is the kb-board branch and a pull request is tied to its card by head branch alone. Continued attempt 1's work on origin/worktree-kb-304-1 rather than restarting, as the operator asked. PR #331 on worktree-kb-304-3, 1333 tests green, lint and tsc clean.\n\n**Attempt 1's recovered WIP was accurate, and is kept.** The 13 mid-flight wiki files in 51f13d1 read correctly against the code; they have been verified, finished and re-stamped rather than replaced. The WIP commit itself is left in the history instead of squashed away — the branch had never been pushed, so rewriting the operator's own recovery commit bought nothing a squash merge does not.\n\n**The window the card had to close was still open, and quieter than a stack trace.** storeKind now reads an absent `store` key as *local*, so this repository's own board — which has no key — resolved to a local board that was never created and read as **empty**: `hkb list` printed \"(no tasks)\" and exited 0, and the dispatcher would have ticked over nothing indefinitely. Attempt 1 had done the loud half correctly (a board.json naming `\"store\": \"github\"` is refused by name with the migration), but that branch is unreachable for an unmigrated board, which has no key at all. `listTasks`/`getTask` (src/store/git.js) now refuse a missing branch with one sentence — `noBoardHere` (src/model.js) — naming all three ways across, because the three ways to be boardless have three different fixes: `hkb init`, `hkb init --import`, and the fetch. The six copies of \"run hkb init\" on the write path say it too, so there is one voice. Verified against this repo: `hkb list` and `hkb show 304` now print the sentence and exit 2.\n\n**The second Done-when was asserted of the decision, not of the tick.** `reconcileDecision` had unit tests against a hand-built listing and nothing exercised the pass through a real tick. There is now a dispatch test that seeds a merged PR on `kb-7-1` through the forge double, plus a card whose branch nobody merged and a merged PR belonging to no card at all, so the branch match is what is being tested rather than the pass running; it also asserts idempotence on the second tick.\n\n**This card broke /kanban:specify, and that needed a verb.** SKILL.md told the operator to write a card's prose with `gh api issues/<n> -X PATCH -F body=@…` in three places — correct while the board was issues, and now an edit to the wrong object entirely, silently, on a repository whose issues may be something else. `updateBody` had been on the Store interface all along with no CLI verb above it. `hkb edit <n> --body-file <p>` (and `--body`) is that verb: it keeps the machine block whatever the driver keeps it in, refuses one body across several cards, and names the fix when the file cannot be read. Building it surfaced a genuine divergence in the double — `FakeStore.setKb` defaulted its `bodyText` to the *passed task's* prose, which modelled the deleted GitHub driver (kb and prose shared one body field) and put pre-edit prose back over a body that had just landed. The double now matches the one driver left, and the conformance scenario pins the pairing in both directions.\n\n**Leftovers cleared.** Comments naming src/tasks.js, src/lock.js, src/store/github.js and `fillPrFallback` now name where those symbols live (src/forge.js, src/model.js) or read as history; protocol.md's remaining issue-isms — \"label the root kb:status:running\", \"its issue closes when its PR merges\", \"a goal issue\", \"oldest issue first\" — say card and status; two up.test.js signal tests were seeding their card on the forge double, which stopped being the board this release, and now take the store double. `lock_sha` survives as an index column and says why: nothing writes it, and dropping it would make the index lose a field that run records written before ADR-006 carry.\n\n**For the operator, before merging.** The card's contract says not to migrate this repository's board and this branch does not. The failure is legible now rather than silent, but it is still a window: the moment this lands on main, driving this board needs `hkb init --import` — and #330's fix to the import's single-page cap, measured at 31 of 131 closed cards that would not migrate today. The PR body says so under its own heading.\n\nNo Co-Authored-By trailer and no \"Generated with\" line on this round's commits or the PR body, per CLAUDE.md. The five commits inherited from attempt 1 are untouched — stripping their trailers is a force push, which the protocol forbids without qualification."
    },
    {
      "artifacts": [],
      "at": "2026-09-03T11:48:56Z",
      "attempt": 5,
      "kind": "result",
      "metadata": {
        "changed_files": [
          "src/forge.js",
          "src/dispatch.js",
          "src/lifecycle.js",
          "src/init.js",
          "src/doctor.js",
          "src/cli.js",
          "src/serve.js",
          "src/board.js",
          "test/dispatch.test.js",
          "test/init.test.js",
          "test/gc.test.js",
          "test/doctor.test.js",
          "test/orphaned-prs.test.js",
          "test/cli.test.js",
          "docs/harnesses.md",
          "docs/wiki/architecture/overview.md",
          "docs/wiki/architecture/store-seam.md",
          "docs/wiki/architecture/local-store.md",
          "docs/wiki/architecture/kb-board-branch.md",
          "docs/wiki/concepts/worker-identity.md",
          "docs/wiki/features/review-loop.md",
          "docs/wiki/features/auto-merge.md",
          "docs/wiki/log.md"
        ],
        "residual_risk": [
          "The tick claiming nothing while the forge is unreachable is a deliberate stall, not a degradation: a board whose `gh` is broken for an hour dispatches nothing for that hour. That is the conservative side of the trade (the alternative is a duplicate PR per ready card per tick), but it is a new way for a board to go quiet, and the only signal is `summary.forge_error` plus the log line. If that turns out to be the wrong default, the narrower rule is to guard only cards whose run record already names a PR.",
          "The `updatedAt > mergedAt` rule for a reopened card depends on the store's `updatedAt` moving on every write. The local driver's does (it is stamped on the card record) and the double's does; a future driver whose `updatedAt` is coarse — or a card reopened in the same second the PR merged — would let the reconcile pass win. A ledger of already-reconciled PR numbers would be exact; this is one comparison and no new state.",
          "The live-worker guard reads a pid, so it only protects an attempt whose `host` is this machine. An attempt running on another host with a merged PR is still reconciled — its claim is protected by the reclaim clock instead, which is a longer leash. Stated on concepts/worker-identity.",
          "`mergedPrsByHead` now reads the `state: 'all'` listing, which pages up to ten times where the old single-page `state=closed` read did not. On a repository with thousands of PRs a reconciling tick can therefore cost more requests than before even though it costs fewer *listings* — the win is real on a gc tick (one listing instead of two) and on a repo under ~100 open+closed PRs, and the new test pins the listing count, not the page count.",
          "`hkb merge` sweeping means an operator merging a card whose worker is running on another host will have that worktree removed (the `keep` filter can only check local pids). The tick had the same limitation; it is now reachable one tick earlier."
        ],
        "verification": [
          "npm run lint && npm test — 1344 tests, 1344 pass, 0 fail (11 new); npm install first so tsc actually runs, and it is clean",
          "item 1 mutation: fillPrs re-throws unconditionally → 'a forge that cannot be reached degrades the tick rather than aborting it' fails",
          "item 2 mutation: resolveStore ignores `existing` → both 'a plain hkb init over a board still on the GitHub store' and 'resolveStore agrees with storeKind' fail",
          "item 3 mutation: the live-worker check disabled → 'a merged PR leaves a live worker its claim' fails",
          "item 4 mutation: the updatedAt > mergedAt check disabled → 'a card moved after its PR merged is left alone' fails",
          "item 5 mutation: mergeCard's sweepTask stubbed out → 'hkb merge sweeps the card it just finished' fails",
          "item 9 mutation: mergedPrsByHead restored to its own state=closed listing → 'the tick pays for one listing per state' fails",
          "items 6-8, 10 are covered by four new tests (the unreadable-card verdict, the two claim-probe cases, the --body refusal) which pass on the fix and describe the old behaviour in their prose",
          "grep -rn 'Closes #' src skills — empty; grep for kb:status/kb:agent/refs/kb/locks in src — nothing outside src/bridge/github-issues.js (the --import path) and the label columns the local driver owns",
          "node .repolore/scripts/wiki-check.mjs — the four pages the card names, plus store-seam, local-store and kb-board-branch, are fresh; 18 remain stale and all 18 were stale before this branch",
          "git fetch origin main && git merge origin/main — already up to date; pushed to worktree-kb-304-3 (457f943), no force, no second PR",
          "No Co-Authored-By or 'Generated with' trailers on this round's three commits"
        ]
      },
      "pr": 331,
      "pr_continued": true,
      "summary": "All 15 review items fixed, each of the six the reviewer required a test for pinned by one that fails when the fix is mutated back. 1344 tests green (11 new), lint and tsc clean, PR #331 updated on worktree-kb-304-3 (457f943). No new PR, no force push, origin/main already merged.\n\n**The two that inverted the card.**\n\n1. *The card that retires GitHub had made every local board read require GitHub.* `fillPrs` sits on the path of `hkb list`, `show`, `claim`, `context`, `graph`, `track`, `promote`, `gc --yes` and the tick, and its catch re-threw — so `hkb list` with `gh` logged out exited non-zero while `hkb init` printed, in the same branch, that the board \"works with `gh` logged out, on a plane, and at no API cost\". A pull request is an *enrichment* of a card, so a failed listing now leaves `prs: []` and records why on the context (`prsUnavailable`). The three callers that cannot honestly act on \"no PR\" — `complete` (it would record a protocol violation for work sitting in a PR it could not see), `requestReview`, `mergeCard` — pass `required: true` and still throw. The tick is the third answer: it degrades but declines to *decide*, claiming nothing while the `active_pr` guard cannot be judged, because an empty `prs` there means \"not known\", not \"none\", and reading it as \"none\" opens a duplicate PR. `summary.forge_error` and a log line say so.\n\n2. *A plain `hkb init` over an unmigrated board silently abandoned every card.* `resolveStore` takes `existing` again and refuses a board.json still saying `\"store\": \"github\"`, with `storeKind`'s own message naming `hkb init --import` — one wording for the decision, not three. The refusal happens before anything is written: the key survives and no empty `kb-board` branch is created. `--import` is the one way through. Covered by a plain-`hkb init`-over-`{store:'github'}` test asserting the key and the branch survive, and by six new pure `resolveStore(flags, existing)` cases.\n\n**Lock and lifecycle (3-5).** The reconcile pass skips a card whose open attempt is still alive on this host (`openAttempt` + `pidAlive`), reporting it as `reconcile_left: worker_alive` rather than silently — releasing it made the worker's next heartbeat LOCK_LOST and cost the board its terminal verb. It also skips a card whose `updatedAt` is newer than the PR's `mergedAt`: a card moved *after* the merge was moved by a human (`request-changes`, `unblock`, `adopt`), and the old GitHub rule that protected it — the issue had to be CLOSED — went with the store. `mergeCard` runs `sweepTask` itself with the same `keep` the tick uses, because setting `done` takes the card out of `RECONCILE_STATUSES` and `sweepFinished` is driven by that pass; the result carries `cleaned` for `--json`.\n\n**Doctor (6-8, 13).** `checkOrphanedPrs` counts and names the cards it could not read and suppresses the `ok` — it printed \"all on cards still open\" from reads that all threw, which is exactly what the migration window produces. `checkClaimLock` computes the index path and `existsSync`es it before touching the lazy getter, so it no longer creates the index it diagnoses; it cannot use a read-only handle (`BEGIN IMMEDIATE` is a write), which is stated where it matters. `BEGIN IMMEDIATE` and `ROLLBACK` get a `try` each. `checkLocalStore` warns rather than vanishing when `kind !== 'local'`. Dead imports (`classifyClaimError`, `remoteName`, `API_VERSION`), the unused `storeKindOf`, and the docblock describing the deleted GraphQL/lock-ref probes are gone.\n\n**Also (9-12, 14-15).** `mergedPrsByHead` filters the memoized `state: 'all'` listing instead of issuing its own `state=closed` read — a test pins that the tick asks at most one listing per state and never a `closed` one. `hkb edit --body` with no value is refused with the fix named (`--body \"\"` still empties a card, and is tested). `dropPrCaches(ctx)` is one exported helper, used by the tick and by both of `serve.js`'s invalidation points, so `prsByHeadAll` can no longer survive a write. `DEFAULT_PROFILES` drops the dead `heartbeat` key and its comments from all five profiles. `docs/harnesses.md`'s duplicated protocol clause, its `KB_LOCK_REF` line, its `\"heartbeat\": \"comment\"` advice and the Codex sandbox note that called the heartbeat a network operation are all rewritten.\n\n**Wiki.** The four pages the card names plus the three store pages are refreshed with the behaviour that moved and re-stamped: the forge as an enrichment (overview, store-seam, review-loop), the claim a merge leaves alone and the human a merge does not undo (worker-identity, auto-merge), the init that refuses and the probe that does not create what it diagnoses (local-store). One `log.md` entry. 18 pages remain stale; all 18 were stale before this branch."
    },
    {
      "artifacts": [],
      "at": "2026-09-03T11:58:30Z",
      "attempt": 7,
      "kind": "result",
      "metadata": {
        "changed_files": [
          "src/init.js",
          "src/bridge/github-issues.js",
          "test/init.test.js",
          "test/fake-gh.js",
          "README.md",
          "docs/wiki/architecture/local-store.md",
          "docs/wiki/architecture/store-seam.md",
          "docs/wiki/log.md",
          "docs/wiki/index.md"
        ],
        "residual_risk": [
          "The probe is one page of 100. A board with more than 100 cards is reported as 'at least 100' rather than its real size — enough to refuse, not enough to report. Paging it would cost requests on a path whose only question is yes/no.",
          "The probe costs one REST call on a plain `hkb init` over an unkeyed board.json, on a command that could previously run entirely offline. It fires only while the board is unkeyed and un-branched — after one successful init the branch exists and it never fires again — but a repo kept deliberately unkeyed will pay it every time, and offline will now refuse rather than proceed. `--force` is the escape hatch, and it is the trade the reviewer asked for: creating the branch is irreversible.",
          "`--no-labels` over an *existing* unkeyed board.json is no longer offline: the probe runs and, unreachable, refuses. The documented offline path (a fresh repo with no board.json) is untouched and tested, but an operator repeating `--no-labels` on a half-set-up repo whose first run failed before writing board.json is fine, while one whose first run wrote board.json without the key would need `--force`. I judged the silent-abandonment risk to outweigh that; it is one flag away either direction.",
          "`countBoardIssues` swallows every error into `{reachable: false}`, including a genuine bug in the request. The message is carried through into the refusal so it is never invisible, but the verdict cannot tell 'logged out' from 'this code is broken'.",
          "The reviewer's hold on merging PR #331 stands and is unaffected by this: merging still makes this repository's board unreadable until it is migrated, and that call is the operator's. What has changed is that the recovery command is now safe — `hkb init` refuses and names `hkb init --import` instead of creating an empty board over the cards."
        ],
        "verification": [
          "npm run lint && npm test — 1349 tests, 1349 pass, 0 fail, 11.2s (5 new); tsc clean after npm install",
          "mutation: `if (false) await refuseUnmigratedBoard(...)` → the unkeyed-board refusal, the unreachable-forge refusal and the --force test all fail; 'a repo with no kb cards on it inits clean' correctly still passes",
          "test 'a plain `hkb init` over an unkeyed board.json with cards on the forge refuses, naming the count' — 2 kb:board:default issues (one open, one closed) plus one plain `bug` issue that must not be counted; asserts exit 2, the count, `hkb init --import`, and that neither the branch nor the store key was written",
          "test 'a repo with no kb cards on it inits clean, --no-labels included' — an unkeyed board over a repo with only ordinary issues inits, and a genuinely fresh repo takes --no-labels with no probe at all",
          "test 'a forge that cannot be reached refuses with its own sentence' — a 401 injected on GET issues; asserts the offline wording, `gh auth status`, that it is NOT the migration sentence, and that no branch was created",
          "test '--force creates the local board anyway, and says what it is walking away from' — branch created, and the log line names the card count and 'stay on GitHub'",
          "test 'needsMigrationProbe and migrationVerdict' — the six conditions and the five verdicts as pure calls, including the capped 'at least 100'",
          "node .repolore/scripts/wiki-check.mjs — architecture/local-store and architecture/store-seam are fresh; the 18 that remain were stale before this branch, and none of them is a page this card names",
          "git fetch origin main && git merge origin/main — already up to date; pushed 0c4e2e6 to worktree-kb-304-3, no force"
        ]
      },
      "pr": 331,
      "pr_continued": true,
      "summary": "Round 3's one item is closed: `hkb init` now refuses to create a local board over an unmigrated one that declares nothing.\n\n**The shape the guard missed.** `resolveStore(flags, existing)` guards on `existing?.store !== undefined && existing.store !== 'local'`, which answers for a board.json that *says* `\"store\": \"github\"`. This repository's board.json — and every one written before the key existed — says nothing at all: absent resolves to `local`, `pinStore` is false so nothing is written, and `setUpLocalBoard` created an empty `kb-board` branch beside 188 cards still on the forge. Empty `hkb list`, no message, from the command an operator runs *because* `hkb list` went quiet.\n\n**The fix, in three pieces.** `needsMigrationProbe({flags, existing, branchExists})` (pure, `src/init.js`) is the four conditions: an existing config, no `store` key, no `--import`, no `kb-board` branch yet. When they hold, `refuseUnmigratedBoard` runs *before* step 2 writes anything — the repo is known from step 1, the branch is checked with one `git rev-parse` — and asks the forge once through `countBoardIssues` (`src/bridge/github-issues.js`, the bridge's read half): a single `GET /issues?labels=kb:board:<slug>&state=all&per_page=100`, `pull_request` rows dropped, and deliberately not `fetchBoard`. It is the one read in that file that never throws — an unreachable forge is an *answer* (\"could not check\"), and what to do with it is the caller's. `migrationVerdict(probe, {board, force})` (pure) decides: reachable-and-empty is `null` (a fresh repository; proceed silently); cards found is exit 2 naming the count — `at least N` when the page came back full, never a number it does not know — and `hkb init --import`; a forge that could not be asked is exit 2 in its own sentence, naming `gh auth status`, because creating the branch is the irreversible half and \"could not check\" is not \"there is nothing there\". `--force` proceeds in both cases and logs what it is walking away from.\n\n**All four of the reviewer's `Done when` rows have a test.** Three of them fail when the guard is taken back out (verified by mutation: `if (false) await refuseUnmigratedBoard(...)`); the fourth is the negative case and correctly still passes. A repository with no board.json is never probed at all, which is what keeps `hkb init --repo owner/name --no-labels` — the offline adoption path — working on a fresh repo, and there is a test for that too.\n\nOne test-double change: `test/fake-gh.js`'s existing `/issues` route now honours `labels` (AND-of-all, the way GitHub reads it). No new route — it is still forge routes plus the bridge's read half.\n\nDocs: README's migration section gained the refusal; `architecture/local-store` gained \"And a board that declares nothing is asked about\" under \"Which store a board is on\"; `architecture/store-seam`'s init paragraph and its list of what survives in the bridge were extended; both re-stamped, `log.md` appended.\n\nPR #331 updated on `worktree-kb-304-3` (0c4e2e6). No new PR, no force push, `origin/main` already merged."
    },
    {
      "artifacts": [],
      "at": "2026-09-03T15:45:14Z",
      "attempt": 9,
      "kind": "result",
      "metadata": {
        "changed_files": [
          "src/bridge/github-issues.js",
          "src/model.js",
          "src/store/index.js",
          "src/store/git.js",
          "src/store/local.js",
          "test/fake-store.js",
          "test/store.test.js",
          "test/store-local.test.js",
          "README.md",
          "docs/wiki/architecture/local-store.md",
          "docs/wiki/architecture/overview.md",
          "docs/wiki/concepts/worker-identity.md",
          "docs/wiki/features/review-loop.md",
          "docs/wiki/features/auto-merge.md",
          "docs/wiki/log.md"
        ],
        "residual_risk": [
          "CLOSED_MAX is 5000 and the migration is a full read of that window, so a board with thousands of closed cards inside 90 days now costs up to 50 GraphQL queries where it used to cost one — plus one comments read per card, which was already the dominant cost and is unchanged. The window, not the page, is what bounds it.",
          "The cap probe costs one extra query, and only when the ceiling is actually reached.",
          "`tagCapped` is a non-enumerable property on the returned array, the same trick `tagBlockers` uses. A caller that copies the list (`[...closed]`, `.filter(...)`) loses the flag; `importGithubBoard` reads it off the driver's own answer before filtering, and the interface doc says absence means 'not cut short'.",
          "The `since` window is applied by every driver now. A caller that passed `first` expecting a page size gets a total ceiling instead — inside this repo the only callers are the migration and the conformance suite, both updated, but a future driver author reading the old signature could be surprised; the typedef in src/store/index.js is where the meaning is written down.",
          "Unchanged from earlier rounds: this PR is held by the operator, since merging it makes this repository's board unreadable until `hkb init --import` runs. That import is now the one that carries all 131 closed cards rather than 100.",
          "The five commits pushed on this branch before attempt 5 still carry Co-Authored-By/Claude-Session trailers; removing them means rewriting pushed history, which the protocol forbids. This round's commits carry none."
        ],
        "verification": [
          "npm run lint && npm test — 1353 tests, 1353 pass, 0 fail, ~10.7s (4 new: three import tests plus one conformance scenario)",
          "fixture larger than one page: 'every card closed inside the window is imported, however many pages that takes' seeds 250 cards closed in the window plus 3 outside it and asserts summary.closed === 250, closed_capped false, 250 CLOSED cards on the branch, the out-of-window card absent, and next_id === 450 (computed after the whole read)",
          "mutation for that test and for the real-ceiling test: `if (true || past || !conn.pageInfo?.hasNextPage) break` (the pre-fix one-query shape) → both fail; restored, both pass",
          "mutation for the false-positive test: replacing the one-node probe with `capped = true` (the pre-fix 'a full page means a cap' shape) → 'a ceiling with nothing but history behind it is not a cap' fails; restored, passes",
          "test/store.test.js conformance: 60 pass — the new `since`/`first` scenario runs on the local driver and the double, so the two cannot disagree about what the migration's window means",
          "item 4 re-verified rather than assumed: `git show` of adoptOpenIssues plus its two tests (issues_capped true on a real cap, false on an exact multiple with asked === [1,2,3])",
          "node .repolore/scripts/wiki-check.mjs — the four pages the card names (architecture/overview, concepts/worker-identity, features/review-loop, features/auto-merge) plus architecture/local-store are fresh; 21 stale remain, the same set that was stale before this round",
          "git fetch origin main && git merge origin/main — one README.md conflict, resolved by hand; pushed to worktree-kb-304-3 (53ecf5a), no force"
        ]
      },
      "pr": 331,
      "pr_continued": true,
      "summary": "Round 4's one item is closed: the migration's closed-card read pages to its window instead of to a page, and `closed_capped` now means a ceiling that was really reached.\n\n**What was wrong.** `fetchClosedRecent` was one GraphQL query — its own comment said \"One query, no paging\" — so the page size was the ceiling by accident. On the board about to be migrated that is 131 cards closed inside the 90-day window and 31 that would not have moved: their run records, results and handoffs left on GitHub while the local board became the source of truth without them, with `next_id` computed from the truncated set.\n\n**The fix.** The read pages on `pageInfo.hasNextPage`/`endCursor`, ordered by `updatedAt` descending, and stops at the first card older than `since` — the window is the scope the migration was given, where the page size never was. `first` became a total ceiling rather than a page size, which is the meaning `src/store/git.js` and `test/fake-store.js` always gave it (`.slice(0, first)`), and both now honour `since` too; the interface's typedef says so and a new conformance scenario pins it on every driver. `importGithubBoard` asks for `{first: CLOSED_MAX (5000), since: <cutoff>}` — the window is the real scope, `CLOSED_MAX` the runaway stop far above any board a migration will meet — and keeps its own window filter behind the driver's, so a `from` double cannot widen the scope by accident. `closedMax` is an option so a test can set it to 5.\n\n**`closed_capped` is now a fact, not a guess.** A full page proves nothing about what follows it, so when `CLOSED_MAX` is reached the driver spends one extra one-node read and reports the cap only if that card is still inside the window — the same \"a ceiling is only real if there is something above it\" rule the adoption path's `more` follows. Drivers say so on the list they return (`tagCapped`/`wasCapped`, `src/model.js`, non-enumerable like `tagBlockers`'s provenance note): a count cannot say whether it is the whole answer, and only the reader that did the paging knows. The warning is now flat (\"stopped at N closed card(s) … they are NOT on the local board\") rather than a \"there may be more\".\n\n**Item 4 (adopt's `more` on an exact multiple) was already fixed** on this branch by round 2 — `adoptOpenIssues` reads the page after its last full one and `test/store-local.test.js` has \"a repository with exactly a whole number of pages is not told cards were left off\" asserting `asked === [1,2,3]` and `issues_capped === false`. Nothing to do, verified rather than assumed. **Item 5**: `BOARD_PROBE_PAGE` keeps its single page and now carries a comment saying why — it answers `hkb init` a yes/no about whether creating the branch would strand cards, not the set of cards a migration writes, and it runs with the operator waiting.\n\n**Also**: `origin/main` moved while this ran (238a3b6, which withdrew the friend-clones-the-repo requirement) and conflicted on README.md — resolved keeping this branch's unmigrated-board paragraph under main's renamed heading, and `architecture/local-store`'s sync section now says sync is a durability path, not a collaboration one. No force push, no second PR."
    },
    {
      "artifacts": [],
      "at": "2026-09-04T03:54:57Z",
      "attempt": 11,
      "kind": "result",
      "metadata": {
        "changed_files": [
          "src/model.js",
          "src/store/index.js",
          "src/store/git.js",
          "src/store/local.js",
          "src/init.js",
          "src/doctor.js",
          "src/board.js",
          "src/cli.js",
          "test/init.test.js",
          "test/doctor.test.js",
          "test/model.test.js",
          "test/store-git.test.js",
          "test/store-local.test.js",
          "test/store.test.js",
          "test/gc.test.js",
          "README.md",
          "CLAUDE.md",
          "AGENTS.md",
          "templates/doc-section.md",
          "skills/kanban/SKILL.md",
          "skills/kanban/references/protocol.md",
          "docs/wiki/architecture/overview.md",
          "docs/wiki/architecture/local-store.md",
          "docs/wiki/architecture/store-seam.md",
          "docs/wiki/architecture/board-ref.md",
          "docs/wiki/concepts/store.md",
          "docs/wiki/concepts/worker-identity.md",
          "docs/wiki/features/review-loop.md",
          "docs/wiki/features/auto-merge.md",
          "docs/wiki/log.md"
        ],
        "residual_risk": [
          "`hkb heartbeat 304` could not run from this worktree: this repository's own board is still on the retired GitHub store, and `main`'s hkb — which this branch is — refuses it with `noBoardHere`. That is the known window the card's contract creates, not a new defect; the operator migrates with `hkb init --import` right after the merge. It means this attempt filed no heartbeats.",
          "`docs/wiki/decisions/adr-006-local-store.md` still says the durable tier is a branch called `kb-board`. Left as written on purpose: #334 left it too, on the ground that an accepted decision record is immutable and the decision (a dedicated git ref, one writer, sync is git) did not change. It is the one place in docs/ where the old vocabulary survives, and `wiki.config.yml` glosses it.",
          "`main`'s `storeKind` refused to infer the store from a ref, and this card makes an absent key mean `local` unconditionally. The dangerous shape that combination allows — a repository whose cards never left the forge, read as an empty local board — is caught by `refuseUnmigratedBoard` at `hkb init`, and only there. A board that reaches `gc.sweep` without ever passing through `init` would still read empty; `sweep` sweeping nothing on an empty board (#294's fix) is what stands behind it.",
          "Two of main's tests were deleted rather than adapted because they assert GitHub-store behaviour this card removes. If any of that behaviour is ever wanted back, the coverage has to be rewritten, not restored."
        ],
        "verification": [
          "git merge origin/main — 14 conflicted files resolved by hand, no rebase, no force-push; pushed 53ecf5a..e12e570 to worktree-kb-304-3 (PR #331)",
          "git log origin/worktree-kb-304-3..origin/main — empty: #334 is fully carried",
          "npm run lint && npm test — 1366 tests, 1366 pass, 0 fail, 13.6s; tsc clean (npm install first)",
          "scratch repo, `hkb init --repo o/r --no-labels`: `git branch -a` lists only `master`; `git for-each-ref | grep kb` shows `refs/kb/boards/default` — #334's central property holds on this branch",
          "same scratch: `hkb doctor` green on store (`local — refs/kb/boards/default`), board ref, board refspec (`+refs/kb/boards/*:refs/kb/remotes/origin/boards/*`), board index, index filesystem — the destination is still hkb's own namespace, not refs/remotes",
          "same scratch: `hkb create` then `hkb list` shows the card — the merged store works end to end, not only under the test doubles",
          "the three `refuseUnmigratedBoard` tests (unkeyed board with forge cards / unreachable forge / --force) pass again after `unmigrate()` was fixed to delete both refs; they failed loudly first, which is what proved the probe was being skipped",
          "node .repolore/scripts/wiki-check.mjs — the eight pages the two cards share are no longer stale; 18 remain, all stale before this branch"
        ]
      },
      "pr": 331,
      "pr_continued": true,
      "summary": "Round 5 was the merge, not a review round. `origin/main` (3e17acd, #334 — the board moved off `refs/heads/kb-board` to `refs/kb/boards/<slug>`) is merged into `worktree-kb-304-3` and pushed to PR #331 as e12e570. No rebase, no force. 1366 tests green, lint and tsc clean, `git log pr331..origin/main` is now empty.\n\n**Both halves verified on a scratch board, not just in tests.** `hkb init` there: `git branch -a` shows only `master`, `refs/kb/boards/default` exists, `hkb doctor` reports `store: local — refs/kb/boards/default`, `board ref … never pushed to origin`, and `board refspec: remote.origin.fetch carries +refs/kb/boards/*:refs/kb/remotes/origin/boards/*` — #334's four properties (hidden ref, refspec into hkb's own namespace, destination matching, per-slug ref) all intact. `hkb create` / `hkb list` work on it.\n\n**14 conflicted files, and which side won each.**\n\n*The board-missing sentence (the collision the reviewer named).* Mine won on structure, main's on vocabulary. `noBoardHere` (`src/model.js`) now takes `{ref, root}` instead of `{branch, root, remote}` and reads \"there is no board at `refs/kb/boards/default` in `<root>`\" — main's sentence — then keeps my three ways across, with **`hkb sync` replacing the raw `git fetch origin kb-board:kb-board`**. That is not cosmetic: a hidden ref is not carried by a default clone and the fetch line the old sentence printed would not have brought the board; `sync` fetches the namespace explicitly and writes the refspec. There is now one vocabulary, not three: the skill, `protocol.md`, README, CLAUDE.md, AGENTS.md and `templates/doc-section.md` all say the board is a ref, and `noBoardHere()` is asserted not to contain the word \"branch\" at all.\n\n*`localBoardExists` / `findLocalBoardRef` (the other named collision).* Both properties kept, and one improvement: `refuseUnmigratedBoard` (`src/init.js`) used `rev-parse BOARD_REF`, which is the *default* slug and knows nothing about the legacy ref. It calls `findLocalBoardRef` now, so a board on `--board beta` or still on `refs/heads/kb-board` counts as a board and the operator is not told to migrate cards they already migrated. #334's old-ref probe and remote derivation are untouched.\n\n*`src/store/index.js`* — mine (github is gone), main's ref wording. I kept the *history* from main's `storeKind` docblock rather than deleting it: the three destructive interactions that followed inferring a store from a ref (an `--import` that deleted live workers' lock refs, a `gc.sweep` that read `[]` and destroyed worker worktrees) are still written down, ending at the guard that replaced them.\n\n*`src/store/git.js`* (5 hunks), *`src/store/local.js`* (3), *`src/doctor.js`*, *`src/init.js`* (4), *`src/board.js`*, *`src/cli.js`* — mine on substance, main's ref plumbing kept whole (`boardRef`, `trackingRefFor`, `boardFetchRefspec`, `isBoardFetchRefspec`, `--create-reflog`, `LEGACY_BOARD_REF`, `remoteFor`, `hasFetchRefspec`, `legacyBoardFix`, the `REFSPEC_CHECK` doctor row). `local.js` took main's whole import list plus my three model imports, and main's `ref: s.ref` in both migration summaries plus my `closed_max`.\n\n*`boardRef`'s refusal* pointed at `--store github` as the escape hatch for an unusable board name. That store does not exist, so it now names a board name that works: `hkb init --board my-board`.\n\n*Tests* — 6 files. Two of main's tests were deleted because they assert the GitHub store's behaviour (`doctor on a GitHub board whose name is not a ref path`, `a GitHub board may be named anything`); the ref-name half of the second is re-added as its own init test. `unmigrate()` in `init.test.js` deleted `refs/heads/kb-board` only, so the three migration-refusal tests stopped exercising the probe — it deletes both refs now. `noBoardHere`'s test rewritten for the ref. `sync pushes the branch, and a clone reads…` expected `listTasks()` to answer `[]` on an unfetched clone; it asserts the refusal instead, which is this card's \"an empty board and a missing one are different facts\".\n\n**Wiki.** The eight pages the two cards share are refreshed and re-stamped: `architecture/overview` (the store paragraph, the three refusals, the seam section that still named `src/store/github.js` in the present tense), `architecture/local-store`, `architecture/store-seam`, `architecture/board-ref`, `concepts/store`, `concepts/worker-identity`, `features/review-loop`, `features/auto-merge`. `wiki-check` reports none of them stale; the 18 that remain were stale before this branch. One `log.md` entry records the merge and which side won each collision."
    }
  ],
  "v": 1
}
