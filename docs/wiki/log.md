# Wiki log

Append-only journal of notable wiki operations, newest **last** — one `##`
line each, so future sessions get recency awareness for free. Never rewrite
history.

<!-- Format:
## YYYY-MM-DD — <added|refreshed|superseded|archived> <category/slug> (why)
-->

## 2026-08-27 — added architecture/overview (seeded at init)

## 2026-08-27 — added decisions/adr-004-roles-and-adoption (from the 7-agent roles evaluation; status proposed)

## 2026-08-27 — refreshed architecture/overview (reclaim now honors `manual`; prose already correct, re-stamped)

## 2026-08-27 — refreshed architecture/overview (no-op re-stamp: #66's seats vocabulary sync touched only comments and help strings in cli.js/model.js/context.js; no claim on the page changed)

## 2026-08-27 — refreshed decisions/adr-004-roles-and-adoption (all three consequences shipped as #68/#69/#70; added an "As shipped" section, anchored the two Context citations to `6c0e81f` now that #68/#69 fixed them, seat vocabulary added to GLOSSARY, status proposed → accepted)

## 2026-08-27 — refreshed architecture/overview + decisions/adr-004-roles-and-adoption (no-op re-stamp: #72 narrowed what `hkb init` seeds into board.json; the cli.js/dispatch.js churn is one help string and one skip message, and no claim on either page changed)

## 2026-08-27 — refreshed architecture/overview + decisions/adr-004-roles-and-adoption (no-op re-stamp: #73 changed one help string in cli.js — `hook stop` → `hook stop|pretool`; no claim on either page changed)

## 2026-08-27 — refreshed architecture/overview + decisions/adr-004-roles-and-adoption (no-op re-stamp: #78 added `init --no-labels` (its offline path, for the tarball smoke) — cli.js churn is one BOOL_FLAGS entry and two help lines, and neither page makes a claim about init's flags)

## 2026-08-27 — added features/web-board (#87 made one `hkb serve` hold several checkouts; serve.js and web/index.html were covered by no page at all, so the whole feature was a freshness blind spot)

## 2026-08-27 — refreshed architecture/overview + decisions/adr-004-roles-and-adoption (no-op re-stamp for #87: cli.js gained two `--repos` help lines, model.js three pure board-key helpers, board.js the `makeContextAt` split plus the user-level board list; no claim on either page changed. One anchor fixed on ADR-004 — `src/cli.js:176` → `:181` for the exit-code help line, which the `--repos` lines and #88 shifted; the `6c0e81f`-pinned `cli.js:419` citation is historical and stays)

## 2026-08-27 — added features/auto-merge (#89 made merging board policy: `dispatch.merge.mode` `manual`|`auto`, the tick enabling GitHub's own auto-merge once per PR, and the branch-protection gate doctor hard-fails on — no page covered who lands an agent's PR)

## 2026-08-27 — refreshed architecture/overview for #89 (one claim tightened: "judgment — whether a PR merges — lives outside the loop" now says what `merge.mode: auto` does and does not change, and points at the new page)

## 2026-08-27 — refreshed decisions/adr-004-roles-and-adoption (no-op re-stamp for #89: dispatch.js and protocol.md moved, but the seat decision is untouched — merge authority stays the operator's, and the dispatcher only enables a GitHub mechanism the operator opted into, never a merge of its own)

## 2026-08-27 — added features/planning-commands (#92: SKILL.md advertised `/kanban:specify` and `/kanban:decompose` while nothing registered them — the page records why they are commands and not `hkb` verbs, how one `commands/` source is registered by both the plugin and `hkb init`, and the three checks that now fail before an adopter does)

## 2026-08-27 — refreshed features/auto-merge (no-op re-stamp for #92: `src/doctor.js` gained an unrelated `checkCommands`; every merge-policy claim on the page — `checkMergePolicy` as a hard failure, the branch-protection gate, the ops view — is untouched)

## 2026-08-27 — refreshed decisions/adr-004-roles-and-adoption (#92 moved SKILL.md and protocol.md but not the seat decision, so no superseding record: the planning commands are operator-seat tooling and the dispatcher still holds no LLM. One citation corrected — exit code 4 is `protocol.md:257`, one line lower after the decompose paragraph was reworded)

## 2026-08-27 — added features/update-notice (#93 gave the CLI half of "is this install old": one npm dist-tag GET a day, in doctor and in the dispatcher loop, with the compound false green — a stale CLI whose packaged skill matches the installed one — as the case it exists for. src/registry.js was covered by no page at all)

## 2026-08-27 — refreshed architecture/overview for #93 (one claim added rather than changed: the loop does two things a tick does not — the token and version notices, at most once a day and outside `tick()` because they write state.json. src/doctor.js added to covers, since the page now cites it)

## 2026-08-27 — refreshed features/planning-commands (#93 shifted two anchors and nothing else: `installCommands` `src/init.js:124-132` → `:131-139` (a `packageVersion` helper went in above it) and `checkCommands` `src/doctor.js:42-48` → `:43-49` (one import line). Every claim about the commands is untouched)

## 2026-08-27 — refreshed features/auto-merge + decisions/adr-004-roles-and-adoption (no-op re-stamp for #93: doctor.js gained the version check, dispatch.js two calls in `loop`, board.js one default key; no merge-policy or seat claim changed. One anchor corrected on ADR-004 — the `a.remote || a.manual` line is `src/dispatch.js:486`, not `:427`, which had drifted before #93 touched the file; the `6c0e81f`-pinned Context citations stay as they are)

## 2026-08-27 — refreshed architecture/overview + features/auto-merge + features/update-notice (no-op re-stamp for #99: model.js gained the pure `mergeBoardEntry` and board.js the user-board-list writer (`saveUserBoards`, `mainWorktree`, `registerUserBoard`), all additive. No claim on any of the three pages is about that list — the page that describes it, features/web-board, covers serve.js and stays fresh; it gets the writer when #98 makes one visible to a user)

## 2026-08-27 — refreshed features/web-board for #101 (one claim added: the user-level board list is now live — `reloadBoards` re-runs `serveContexts` at most once per poll interval, on the requests the page already makes, and every board still in the list keeps its state object. Five anchors that had drifted with the new code were corrected)

## 2026-08-27 — refreshed features/planning-commands + features/update-notice (no-op re-stamp for #100: `hkb init` gained a step 7 that registers the checkout it set up in the user-level board list, plus a `--json` payload, and scripts/smoke-pack.mjs now runs every command against a throwaway `KB_CONFIG_HOME` so its scratch repo cannot land on a maintainer's list. No claim on either page is about any of that; one anchor shifted by the new import line — `installCommands` `src/init.js:131-139` → `:132-140`. The page that owns the board list, features/web-board, covers serve.js and is still fresh: the whole "the list maintains itself" story is #102's to write, now that #101 has made serve pick the list up while it runs)

## 2026-08-28 — refreshed features/web-board for #98 (the writing half of the user-level board list, which #99's log line deferred to here: a new "How a checkout gets on the list" section — `registerUserBoard` called once from `hkb init`'s last step, the three properties that make registering without asking defensible (idempotent by resolved path + slug over an append-only merge and an atomic rename, never silent, never fatal), a linked worktree registering its main checkout instead of itself, and why the entry is a bare path and not `path#board`. The "filesystem is never scanned" promise gained the distinction that keeps it true: running `hkb init` in a directory *is* the act of naming it. `src/board.js` and `src/init.js` joined `covers` — the page cited board.js with no freshness anchor before)

## 2026-08-28 — refreshed architecture/overview + features/auto-merge + decisions/adr-004-roles-and-adoption (no-op re-stamp for #98's two synthesis edits: one help line in src/cli.js, and `mergeBoardEntry`'s NUL key separator written as an escape instead of a literal byte, so grep and ripgrep stop treating src/model.js as binary and skipping it. No claim on any of the three pages is about either. One present-tense anchor shifted by the help line — adr-004 `src/cli.js:181` → `:182`; that record's two `6c0e81f`-pinned citations are historical by construction and were left alone)

## 2026-08-28 — refreshed features/web-board for #108 (a new "The drawer draws the subgraph" section: the focused card's dependency subgraph as inline SVG, drawn from the `blockedBy` already on every `/api/board` card — no route, no fetch, no server change. Records the four decisions that make it readable (every edge between the collected nodes, so a diamond comes back; a finished blocker is a node from the stub that names it, since `fetchBoard` reads open issues only; the column palette and the existing `data-open` handler reused; nothing drawn when there is nothing to draw), the layout's accepted limits and the two renderers it may never become — a module `<script>` breaks every vm page test, a CDN breaks the self-containment assertions — and the one case the picture can be wrong: the REST-fallback fingerprint `edgesMayBeMissing` reads off the payload it already has, since widening `fillBlockedByRest` is a per-task call Values 2 and 3 forbid. `covers` unchanged; "Dependency subgraph" added to the glossary)

## 2026-08-28 — refreshed architecture/overview (no-op re-stamp for #119: the Stop hook now writes the session identity onto every attempt the session is answerable for, not only `KB_TASK`'s — a track runner's nodes get `session_id`/`transcript_path` from the pending `.kanban/sessions/<n>-<k>` markers `hkb claim` leaves inside a session, so the set is on disk and costs no board read. The page's one hook claim is that the Stop/PreToolUse hooks enforce the worker guard rails, and that is untouched; the src/cli.js drift it was already stale for is #111's `hkb graph` command, equally not a claim on this page. The story belongs to features/tracks, still waiting to be drafted)

## 2026-08-28 — refreshed architecture/overview for #115 (a real edit, not a re-stamp: "Workers are any harness" said a worker is "pointed at one card with `KB_TASK` set", and for the DEFAULT profile that was never true — `claude --bg` hands the launch to a session daemon that was started long before, so the dispatcher's environment stops at the CLI. Two new paragraphs record the asymmetry and both answers to it: `whichAttempt` reads the attempt back out of the `kb-<n>-<k>` checkout, the same identity `matchJobByWorktree` already matches jobs by; and session identity — `CLAUDE_CODE_SESSION_ID` plus the job record `currentSession` reads — is written by the terminal verb rather than the Stop hook, because the verb is the one thing every worker runs and it is already writing that row. `sessionForAttempt`'s "only an attempt this session ran" rule is what keeps an operator's own terminal out of it and what gives a track's nodes the runner's transcript. `src/jobs.js` added to `covers`; "Transcript" added to the glossary. The page's other claims were re-read and hold; the src/cli.js, src/tasks.js and src/doctor.js drift it was already stale for is #124/#127/#131, none of them a claim here)

## 2026-08-28 — refreshed architecture/overview (targeted, unstamped) for #125: the terminal-verb paragraph now says why the verb needed a second spelling — `complete` is a bash builtin and Claude Code refuses it, and heredocs, in a worktree-isolated session, so `VERB_ALIASES` in src/cli.js resolves `finish` before routing. NOT re-stamped: the page was already stale for #124/#127/#131 drift this task did not verify, and stamping would bless claims it did not read

## 2026-08-28 — refreshed features/web-board (no-op re-stamp for #138 + #143: both edits to `src/board.js` are in the profile/allow-list region — `SAFE_BUILTINS` folded into `CLAUDE_TOOLS`, and a shared `CLAUDE_DENY` behind the launch's `--disallowedTools` — and `src/init.js` gained the `disallowed_tools` template variable. No claim on this page is about either; it owns `hkb serve`, the user-level board list and the dependency drawer. Five present-tense citations had drifted by the inserted lines and were re-read and corrected: `registerUserBoard` :353-359 → :377-383, `saveUserBoards` :315-327 → :339-351, `mainWorktree` :335-342 → :359-366, `registerCheckout` `src/init.js`:603-618 → :609-624, and init's last step :760 → :766)

## 2026-08-28 — refreshed architecture/overview (targeted, unstamped) for #143: "Workers are any harness" said the guard rails are "enforced by the Stop/PreToolUse hooks", which was the pre-#133 story. A new paragraph says what actually enforces — the launch line (`--permission-mode dontAsk`, `CLAUDE_TOOLS` + `CLAUDE_DENY` in `src/board.js`), the one layer live on every profile including the `claude --bg` default the `KB_TASK`-gated PreToolUse hook never reaches — and why `preToolHook` is now deny-or-silent: a hook `allow` overrides Claude Code's own checks, so this layer may only subtract. NOT re-stamped: the page is still stale for the `src/cli.js`, `src/model.js`, `src/dispatch.js`, `src/jobs.js` and `src/doctor.js` drift this task did not read

## 2026-08-28 — added features/review-loop for #153 (the loop the protocol draws did not turn: `hkb request-changes` produced exactly the card the `active_pr` guard parks, so on a board with a dispatcher the verb was a no-op and the reviewer's brief was never read. The page carries the contradiction, the one exemption — `activePrGuard`, keyed on the reviewer's synthetic `changes_requested` row and only while it is the latest — and the two halves that make a relaunch worth having: a checkout the dispatcher makes on the PR's head branch (`worktreeOnBranch`, freeing the ended attempt's worktree, the harness's own worktree flag stripped) and the block in the brief that names the PR and forbids a second one. Glossary gained *Continuation* and *Guard*. The `architecture/dispatcher-tick` page this belongs beside is still planned. Re-stamped after rebasing onto #143, whose `src/board.js` and `src/context.js` edits were read: `CLAUDE_DENY` still carries both force-push patterns, so the page's "the shipped profiles deny `git push --force`" holds, and the two prompt lines #143 added are new ones, so "three standing protocol lines change" is still exactly right)

## 2026-08-28 — added features/up-and-down for #148 (the new hkb up / hkb down verb: pid files as the whole protocol and their two writers — the process itself, and up pre-writing the pid of the child it just spawned so a second up one millisecond later finds a live claim instead of racing; why the server claims but never refuses where the dispatcher genuinely is a singleton; why the child is process.execPath plus this package own bin and carries no KB_* — a daemon that outlives a worker session must not inherit its identity; and the exit record in state.json, which is how --status can report exit 4 without up becoming the supervisor that restarts it. Covers src/up.js, src/model.js, src/board.js, src/dispatch.js, src/serve.js; Exit record and Pid file added to the glossary, Supervisor amended to say up is not one)

## 2026-08-28 — refreshed features/web-board for #148 (one real claim added, not a re-stamp: hkb serve now claims .kanban/serve.pid once the port is bound and drops it on exit, which is what makes it a process hkb up --serve starts and hkb down --serve stops, and the EADDRINUSE message says "already up on that port" only when that file names a live process that is not this one. Its dispatcherState claim was re-read and holds — it now goes through processState in board.js but still reads that checkout own .kanban/dispatch.pid. The other five pages are left unstamped: every one of them was already stale at HEAD for drift this task did not verify)

## 2026-08-28 — refreshed features/up-and-down for the #151 review (a real rewrite of the `down` section, not a re-stamp: `down` used to SIGTERM and `rmSync` the pid file in one breath, which handed the next `hkb up` a board that looked idle while the old loop was still finishing a tick — the reviewer measured two dispatchers running side by side, and the singleton lock could not see it because the lock *is* that file. Both halves are now on the page: the loop's wait between ticks is a `Promise.race` the SIGTERM handler resolves, so a signal in the sleep ends the loop there rather than buying it another tick; and `down` waits for `pidAlive` to go false, bounded by `stopWaitMs`, deletes nothing it did not watch die, and reports what it could not do in `failed[]` plus a non-zero exit. Two new sections: the boot-time staleness guard for pid files a reboot invalidated, and `up`'s 300ms recheck so "started pid N" is never said about a corpse. Every citation on the page was re-read and corrected — the old ones were written at 616f0b7 and had drifted through #138, #143 and this pass. *Stale pid file* added to the glossary, *Pid file* amended to say only the writer deletes one)

## 2026-08-28 — added features/hook-install-shapes for #146 (a new page, per "new feature → new page": the hook command a project commits is now measured, not guessed — whenever the running hkb lives *inside* the repo, `hkb init` writes `f="$CLAUDE_PROJECT_DIR/<path.relative>"; [ -f "$f" ] || exit 0; exec node "$f" hook <verb>` into the *tracked* `.claude/settings.json` with no `--shared-hooks`, which covers both the `npm i -D hkb-cli` devDependency (`node_modules/hkb-cli/bin/hkb.js`) and hkb's own checkout (`bin/hkb.js`). Records what is expensive to rediscover: the `matcher:"*"` blast radius that made the tracked file off-limits in #85 and what changed; why `hkbOnPath` had to stop trusting an npx-augmented PATH and detect by path comparison instead; why the remainder is measured rather than composed from the package name, which a pnpm store or a nested install would have got wrong; why the command guards its own file (a worker's worktree has no node_modules until `npm ci`; #144 is what closes that); doctor's three shape-specific verdicts; and the two pure functions that generalised rather than gaining a special case — `hookCommandNeeds` expanding a `VAR=` assignment, `isHkbHookCommand` accepting a `;`. `related` added on architecture/overview, which was NOT re-stamped: it is stale for drift this task did not verify, and none of its claims are about where a hook command comes from)

## 2026-08-28 — refreshed features/web-board for #146 (no prose claim changed — `src/init.js`'s edits are all in the hook-command section and `src/board.js`'s are `hkbOnPath`, neither of which this page claims anything about — but it was **already stale at origin/main**, not by this branch: its `src/serve.js`, `src/board.js` and `src/init.js` shas still pointed at `cb9e43d` while #148's re-stamp of it never landed. Every one of its sixteen line anchors was re-read against the code and thirteen were corrected, most of them by #148's and #143's insertions rather than this task's: `COLUMNS` :21 → :20, `parseRoute` :196-209 → :219-232, `checkOrigin` :226-240 → :249-263, `portInUse` :621-627 → :622-628, `serveContexts` :128-141 → :151-164, `reloadBoards` :350-372 → :373-395, `keyBoards` :147-156 → :170-179, `boardState` :375 → :398, `MOVES` :33-46 → :32-46, `registerUserBoard` `src/board.js`:377-383 → :525-531, `saveUserBoards` :339-351 → :487-499, `mainWorktree` :359-366 → :507-514, `registerCheckout` `src/init.js`:609-624 → :726-741 with init's last step :766 → :888, and `fillBlockedByRest`'s application site `src/tasks.js:94` → :99. `claimServePid` :122-134 was verified and left. Now fresh)

## 2026-08-28 — refreshed features/up-and-down for #146 (a true no-op re-stamp: this task's `src/model.js` change is a single 39-line insertion at :637 — `insideRepo` and `stripNodeModulesBin`, in a new "where the running hkb came from" section — and its `src/board.js` change is one import plus `hkbOnPath`'s body. No pid-file, exit-record or spawn claim on the page is about either. Every citation below the insertion was shifted by exactly 39 and corrected: `detachedEnv` :788-792 → :827-831, `stopWaitMs` :825-829 → :864-868, `pidFileStale` :812-817 → :851-856, `processLine` :853-867 → :892-906, `startLogLine` :881-883 → :920-922; the `src/board.js` anchors were re-read and had not moved)

## 2026-08-28 — added concepts/worker-identity for #150 (the leak the #125 fallback did not cover: a `claude --bg` launch that finds no session daemon STARTS one, and that daemon keeps the launch environment for life — so `KB_TASK=146 KB_PROFILE=claude KB_ROOT=…` reached every session it hosted, an operator conversation included, whose Stop hook then stamped itself onto #146's attempt row and whose shell was policed by a worker's allowlist. The page collects what was scattered across src/hook.js, src/jobs.js and src/dispatch.js: the three answers to "which attempt is this session?", the order of trust between them, why the environment is scrubbed on that one launch (`scrubKbEnv`), why the judgement is narrow enough to leave `claude -p` and Actions workers untouched (`worksInWorktree`), and why the tick's job identity outranks a hook stamp. "Job record" and "Worker identity" added to the glossary)

## 2026-08-28 — refreshed architecture/overview (targeted, unstamped) for #150: the "which card" paragraph said the launch environment answers for a child process and the checkout for a background agent, which is still true but no longer complete — the two can disagree, and the checkout wins. One sentence, plus concepts/worker-identity in `related`. NOT re-stamped: the page was already stale for src/cli.js/src/tasks.js/src/doctor.js drift this task did not verify

## 2026-08-29 — refreshed concepts/worker-identity for the #150 review's B1 (a real edit, not a re-stamp: `attemptIdentity`'s "nothing here contradicts it" branch trusted the environment from *any* cwd that was neither the board root nor a mismatched `kb-<n>-<k>` checkout — a review worktree, or a session a poisoned daemon went on to host for an unrelated repo, both read as agreement. Fixed so only a `kb-<n>-<k>` checkout naming this exact task and attempt counts as evidence; everywhere else is a leak, the same as the board root always was. `checkEnvLeak` (src/doctor.js) inherits the fix for free — it calls the same function. New tests in test/hook.test.js for both shapes)

## 2026-08-28 — added features/operator-seat for #149 (the third slash command, `/kanban:operate`: the operator's seat written down as a procedure — up, watch, react per event kind, report — because it is the one seat hkb structurally cannot guard. The page carries what the SKILL.md section cannot: that every worker guard (`refuseIfWorker`, `DENY_PATTERNS`, the launch denies) tests `KB_TASK`, which an operator session does not have, so the boundary *is* the brief; that the reaction table is a projection of `EVENT_KINDS` / `STATUSES` / `OUTCOMES` / `BLOCK_KINDS` and a new test in test/skill.test.js fails when a token is added with no row; and the four rows that encode lifecycle behaviour a session would otherwise get wrong — comment before unblock, the two block kinds that ask for nobody, `manual` as a real answer, and request-changes as a relaunch. Glossary gained *Operator session* and an amended *Planning command*. No other page was re-stamped: the ones already stale at HEAD are stale for drift this task did not read)

## 2026-08-28 — refreshed features/planning-commands (targeted, unstamped) for #149: it said `commands/` "holds exactly two flat files", which this task made false — the directory now holds three, and the third is not a planning command, so the sentence names it and links the page that owns it. The two `SKILL.md` section citations (`:147`/`:175`) were re-read and corrected to `:322`/`:350`. NOT re-stamped: the page was already stale at HEAD for `src/init.js`, `src/doctor.js` and `scripts/smoke-pack.mjs` drift this task did not verify

## 2026-08-28 — refreshed features/hook-install-shapes (true no-op re-stamp) for #149: it was fresh at origin/main and only this task's own edits made it stale, all three of them outside anything it claims — two rows appended to `MUST_SHIP`/`FROM_PACKAGE` in `scripts/smoke-pack.mjs` for the new command file, and comment-only rewording in `src/init.js` (the `---------- the slash commands ----------` block) and `src/doctor.js` (`checkCommands`'s docstring). The page owns the hook command's shape, `hkbOnPath`, `checkHooks` and the settings file it lands in; none of that moved

## 2026-08-28 — refreshed features/hook-install-shapes for #144 (a real rewrite of the placement half, and a retraction of it: the page's central table used to answer "which settings file", and the answer is now *neither* — hkb's `Stop` and `PreToolUse` hooks serve only the worker hkb launched, so they ride that worker's launch as `--settings '{"hooks":…}'` and `hkb init` writes no settings file unless `--shared-hooks` asks. New sections: why the launch may name this machine when a tracked file may not (#85 inverted, not contradicted); why `.kanban/board.json` holds the `{hook_settings}` placeholder and never the JSON; the measured `claude --bg` forwarding answer, carried with a `TODO-VERIFY` because it was read out of the shipped 2.1.251 binary's own forwarding table rather than observed at runtime; and doctor's two new checks, `hooks in settings` and `launch hooks` — the latter for a launch frozen in `board.json` before the flag existed, which is the one way this change could go quiet. The command-shape half — measured-not-composed, the guard, the `hkbOnPath` trap, the two parser consequences — survives intact, rescoped to the file `--shared-hooks` writes; the guard's "cost to name in the worker prompt" is gone, because a launch names an hkb that is already installed. architecture/overview gained a paragraph and was NOT re-stamped: it is stale for #124/#127/#131 drift this task did not verify, as it was at origin/main)

## 2026-08-28 — refreshed features/up-and-down for #144 (a true no-op re-stamp: nothing on the page is about hooks, launches or `--settings`. Every anchor sat below one of this task's insertions and was shifted by the measured amount, then each subject re-read at its new line: `src/model.js` +43 (`detachedEnv` :827-831 → :870-874, `pidFileStale` :851-856 → :894-899, `stopWaitMs` :864-868 → :907-911, `processLine` :892-906 → :935-949, `startLogLine` :920-922 → :963-965), `src/board.js` +13 (`readPidFile` :176-183 → :189-196, `recordExit` :192-196 → :205-209, `processState` :212-226 → :225-239), `src/dispatch.js` +12 (`acquireLoopLock` :918-937 → :930-949 and :924/:933/:934-936 with it, `loop` :945-990 → :957-1002, the exit-4 record :991-1000 → :1003-1012, the worker `KB_*` export :128 → :140). `src/serve.js` and `src/up.js` were untouched by this task and their anchors were left)

## 2026-08-28 — refreshed features/web-board for #144 (a true no-op re-stamp for the same reason: this task's `src/board.js` change is the 13-line `HOOK_SETTINGS_VAR` block above `DEFAULT_PROFILES` plus one launch element each in three profiles, and its `src/init.js` change is the hook section — neither is a claim this page makes. Five anchors below those insertions were shifted and re-read: `registerUserBoard` `src/board.js`:525-531 → :538-544, `saveUserBoards` :487-499 → :500-512, `mainWorktree` :507-514 → :520-527, `registerCheckout` `src/init.js`:726-741 → :761-776, and init's last step :888 → :932. `src/serve.js` and `src/tasks.js` anchors were untouched)

## 2026-08-28 — merged origin/main (#144) into #149's branch: `features/hook-install-shapes` and `features/web-board` both took **main's** version whole, since #144 rewrote the first and re-anchored the second against line numbers this branch never saw; the two no-op re-stamps this branch logged above are superseded by those. `features/operator-seat` and `features/planning-commands` are this branch's and were kept. Both surviving pages re-checked after the merge

## 2026-08-28 — refreshed features/hook-install-shapes for #166 (a real addition, not a re-stamp: `installHarness` and `mcpLaunch` — the Codex/Copilot hook writer and the `.mcp.json` writer — called `hkbCommandForHook()`/`projectBinRel()` with no `root`, so on a devDependency or self-checkout repo they always fell through to the machine-specific branch and wrote an absolute path into three *tracked* files, exactly the failure #146 fixed for Claude's settings file but not reached from these two call sites. New section "Codex, Copilot and `.mcp.json` get the same remainder, unguarded (#166)": the same `projectBinRel` remainder, named relative to each harness's own cwd (`relativeHookCommand`, `hkbCommandForHook(verb, { cwd: true })`) instead of through `$CLAUDE_PROJECT_DIR`, since neither harness sets that variable but both already run their hook from the project root; and why it ships with no `[ -f … ] || exit 0` guard — the guard's `f="…";…` is shell syntax, and whether either harness runs `command` through a shell is undocumented, so the unguarded form is the one correct either way, at the cost of a hard failure instead of silence before `npm ci`. `src/mcp.js` added to `covers`; every other citation on the page was re-read and had not moved)
## 2026-08-28 — refreshed features/review-loop and features/web-board for #162 (three gaps #160's review of #153 found, closed: `worktreeOnBranch` reused the *local* branch as `git worktree add` left it, so a human push to the PR since the last attempt was invisible and the continuation's own `git push` would be rejected non-fast-forward — it now runs `git merge --ff-only origin/<branch>` right after the add, best effort and never a force, and reports a genuine divergence as `stale` rather than claiming the checkout current; `spawnWorker` turns that into `continues_branch_stale` on the attempt row and a third, accurate brief variant — checked out on the right branch but not caught up — instead of either the "already there" line or the fresh-branch recipe. Trigger-mode profiles (`claude-action`) never made this checkout at all: the real worker runs elsewhere, on its own `actions/checkout`, so `spawnWorker` now skips `worktreeOnBranch` outright when `profile.mode === 'trigger'` and such an attempt records `continues_pr` only. The page's "Known gaps" section and the checkout paragraphs were rewritten for all three; the *Known gaps* fetch-failure line now names what the ff-only merge does with a fetch that silently failed. features/web-board was a true no-op re-stamp — its `src/board.js` sha moved for `worktreeOnBranch` alone, and the page claims nothing about it. Both back to fresh; the board-wide baseline returns to the 7 pages stale at main before this task)

## 2026-08-28 — refreshed features/operator-seat for #162 (a true no-op re-stamp, but one anchor had actually drifted: this task's `activePrGuard` rewrite in `src/model.js` inserted 6 lines above `DENY_PATTERNS`, moving it from `:724-732` to `:729-736` — the citation was corrected. `src/cli.js`'s `refuseIfWorker:235-238` and `src/lifecycle.js`'s three request-changes/block citations all sit above where this task edited and were re-checked in place, unmoved. No claim on the page is about `activePrGuard`, `claim --spawn`'s new `continuePr` handling, or the CLI wording fix)

## 2026-08-29 — merged origin/main (#144/#149) into #150's branch (PR #152): mechanical conflicts only, no semantic clash — `docs/harnesses.md`'s hook table gained both a "where it comes from" row (#144) and #150's "and the checkout when the two disagree" wording on "what identifies the session"; `src/dispatch.js` kept both import additions (`HOOK_SETTINGS_VAR`/`workerHookSettings` from #144, `scrubKbEnv` from #150 — #144 passes hook settings on argv, not via `KB_*`, so the scrub does not strip it); this file's two branches of entries were concatenated in date order

## 2026-08-29 — refreshed concepts/worker-identity for the #150 review's second pass (real edit: the review's probe — cwd `kb-149-3` with `KB_ROOT=/nonexistent` — found `attemptIdentity`'s shipped rule only compared checkout *basenames*, while this page and `docs/harnesses.md` already claimed the stronger "KB_ROOT joined with kb-<n>-<k> is the only cwd that counts". Implemented the exact compare rather than retracting the claim: `attemptIdentity` (`src/model.js`) now takes `herePath`/`rootPath` — the caller's `path.resolve`d cwd and `KB_ROOT` — and only agrees when the resolved cwd equals `rootPath` joined with `kb-<n>-<k>`; a same-numbered checkout under the wrong (or unset) `KB_ROOT` still falls back to `source: 'worktree'` with a leak note, never `'env'`, so the policy stays inert. `whichAttempt` (`src/hook.js`) and `checkEnvLeak` (`src/doctor.js`) both pass the new params; `docs/harnesses.md`'s claim needed no change, it was already accurate. New tests in test/hook.test.js for the wrong-KB_ROOT and unset-KB_ROOT shapes, at both the pure-function and doctor layers)

## 2026-08-29 — merged origin/main into #166's branch (PR #173): mechanical conflict on this file only (both branches appended a line, concatenated in date order); `src/doctor.js`, `docs/harnesses.md` and `docs/wiki/architecture/overview.md` auto-merged cleanly against #150's and #149's landed changes, no semantic clash with this task's `checkHarnesses`/`checkMcp` additions

## 2026-08-29 — refreshed features/web-board (no-op re-stamp for #150/#166 review pass) — `registerCheckout`'s two `src/init.js` anchors had drifted with unrelated insertions above them (`:769-784` → `:803-818`, `:940` → `:988`); the four `src/board.js`/`src/serve.js` anchors were re-read and had not moved. No claim on the page changed

## 2026-08-29 — refreshed features/hook-install-shapes for the #166 review's second pass (a real edit: `mcpLaunch`'s fallback used to be the same absolute, this-machine-only form `hkbCommandForHook`'s private branch writes, which the page said plainly — "instead of the absolute fallback" — but was never true for a *tracked* `.mcp.json`, only for the launch line. Fixed in code to match: `mcpLaunch` now checks the remainder in the same order as `hkbCommandForHook` (repo-carried hkb first, even over a global one on PATH) and falls back to the plain `hkb` every teammate needs on PATH, same as a harness file's `shared` branch — never an absolute path into a tracked file. New section on `mcpSnippets`: `.vscode/mcp.json` reuses `.mcp.json`'s launch since VS Code resolves it the same way, but `~/.codex/config.toml` is user-level and Codex resolves its `args` against wherever `codex` starts, not the project, so it gets its own `mcpLaunch({ shared: false })` — bare `hkb` or this checkout's own absolute `bin/hkb.js`, the one place that absolute form is still correct. `checkMcp`'s comment on the doctor side was corrected the same way. No line-number anchors in this section to re-check — it cites by function name only)
## 2026-08-29 — refreshed features/review-loop and features/operator-seat for #162's continuation review (merging origin/main — #144/#149/#150/#164 — moved both pages' anchors again). `review-loop`: a real content update, not just anchors — the reuse-existing-worktree path (a spawn that died between checkout and launch) now fetches and fast-forwards too, `worktreeOnBranch` distinguishes "no `<remote>/<branch>` ref to catch up to" (a fetch that never ran or never succeeded once) from a genuine divergence via `git rev-parse --verify --quiet` before the ff-only merge, `hkb claim --spawn` (`src/cli.js`) now records `continues_branch_stale` like the dispatcher's own claim, and the trigger-mode claim log line still names the PR it continues even though it makes no checkout. The Known-gaps sentence about a silent fetch failure was corrected: measured, a failed fetch against an *existing* tracking ref lets the ff-only merge succeed trivially ("Already up to date") and the checkout is reported clean though the remote may be ahead — only a fetch that has *never* created the ref is reported `stale`, and now with its own message rather than the generic ff-only failure. `src/cli.js` added to `covers`. `operator-seat`: a true no-op re-stamp — `refuseIfWorker:235-238` (`src/cli.js`) is unmoved, `DENY_PATTERNS` moved `:729-736` → `:838-846`, and the three `src/lifecycle.js` block/unblock citations shifted a few lines (`:283-293`→`:285-295`, `:294-299`→`:296-301`, `:302-313`→`:304-314`) for insertions above them elsewhere in the file; no claim on the page changed. features/web-board re-stamped again, no-op — its `src/board.js` sha moved for the same `worktreeOnBranch` edit and the page still claims nothing about it

## 2026-08-29 — refreshed features/hook-install-shapes for #182: `effort` joins `model` as a profile field rendered into `{model_args}` (`EFFORT_LEVELS`/`modelArgs` in `src/model.js`, validated in `loadBoard`, `src/board.js`), so a launch is never pinned just to set either. `launch hooks`'s fix (`src/doctor.js` `checkHooks`) now names the surgical repair — insert `{hook_settings}` after `--disallowedTools`, or drop the pin only if it added nothing but --model/--effort — instead of the old blanket "drop launch". No line-number anchors touched; the page cites by function/flag name only

## 2026-08-28 — refreshed features/up-and-down for the #151 review (a real rewrite of the `down` section, not a re-stamp: `down` used to SIGTERM and `rmSync` the pid file in one breath, which handed the next `hkb up` a board that looked idle while the old loop was still finishing a tick — the reviewer measured two dispatchers running side by side, and the singleton lock could not see it because the lock *is* that file. Both halves are now on the page: the loop's wait between ticks is a `Promise.race` the SIGTERM handler resolves, so a signal in the sleep ends the loop there rather than buying it another tick; and `down` waits for `pidAlive` to go false, bounded by `stopWaitMs`, deletes nothing it did not watch die, and reports what it could not do in `failed[]` plus a non-zero exit. Two new sections: the boot-time staleness guard for pid files a reboot invalidated, and `up`'s 300ms recheck so "started pid N" is never said about a corpse. Every citation on the page was re-read and corrected — the old ones were written at 616f0b7 and had drifted through #138, #143 and this pass. *Stale pid file* added to the glossary, *Pid file* amended to say only the writer deletes one)

## 2026-08-28 — added features/hook-install-shapes for #146 (a new page, per "new feature → new page": the hook command a project commits is now measured, not guessed — whenever the running hkb lives *inside* the repo, `hkb init` writes `f="$CLAUDE_PROJECT_DIR/<path.relative>"; [ -f "$f" ] || exit 0; exec node "$f" hook <verb>` into the *tracked* `.claude/settings.json` with no `--shared-hooks`, which covers both the `npm i -D hkb-cli` devDependency (`node_modules/hkb-cli/bin/hkb.js`) and hkb's own checkout (`bin/hkb.js`). Records what is expensive to rediscover: the `matcher:"*"` blast radius that made the tracked file off-limits in #85 and what changed; why `hkbOnPath` had to stop trusting an npx-augmented PATH and detect by path comparison instead; why the remainder is measured rather than composed from the package name, which a pnpm store or a nested install would have got wrong; why the command guards its own file (a worker's worktree has no node_modules until `npm ci`; #144 is what closes that); doctor's three shape-specific verdicts; and the two pure functions that generalised rather than gaining a special case — `hookCommandNeeds` expanding a `VAR=` assignment, `isHkbHookCommand` accepting a `;`. `related` added on architecture/overview, which was NOT re-stamped: it is stale for drift this task did not verify, and none of its claims are about where a hook command comes from)

## 2026-08-28 — refreshed features/web-board for #146 (no prose claim changed — `src/init.js`'s edits are all in the hook-command section and `src/board.js`'s are `hkbOnPath`, neither of which this page claims anything about — but it was **already stale at origin/main**, not by this branch: its `src/serve.js`, `src/board.js` and `src/init.js` shas still pointed at `cb9e43d` while #148's re-stamp of it never landed. Every one of its sixteen line anchors was re-read against the code and thirteen were corrected, most of them by #148's and #143's insertions rather than this task's: `COLUMNS` :21 → :20, `parseRoute` :196-209 → :219-232, `checkOrigin` :226-240 → :249-263, `portInUse` :621-627 → :622-628, `serveContexts` :128-141 → :151-164, `reloadBoards` :350-372 → :373-395, `keyBoards` :147-156 → :170-179, `boardState` :375 → :398, `MOVES` :33-46 → :32-46, `registerUserBoard` `src/board.js`:377-383 → :525-531, `saveUserBoards` :339-351 → :487-499, `mainWorktree` :359-366 → :507-514, `registerCheckout` `src/init.js`:609-624 → :726-741 with init's last step :766 → :888, and `fillBlockedByRest`'s application site `src/tasks.js:94` → :99. `claimServePid` :122-134 was verified and left. Now fresh)

## 2026-08-28 — refreshed features/up-and-down for #146 (a true no-op re-stamp: this task's `src/model.js` change is a single 39-line insertion at :637 — `insideRepo` and `stripNodeModulesBin`, in a new "where the running hkb came from" section — and its `src/board.js` change is one import plus `hkbOnPath`'s body. No pid-file, exit-record or spawn claim on the page is about either. Every citation below the insertion was shifted by exactly 39 and corrected: `detachedEnv` :788-792 → :827-831, `stopWaitMs` :825-829 → :864-868, `pidFileStale` :812-817 → :851-856, `processLine` :853-867 → :892-906, `startLogLine` :881-883 → :920-922; the `src/board.js` anchors were re-read and had not moved)

## 2026-08-28 — added concepts/worker-identity for #150 (the leak the #125 fallback did not cover: a `claude --bg` launch that finds no session daemon STARTS one, and that daemon keeps the launch environment for life — so `KB_TASK=146 KB_PROFILE=claude KB_ROOT=…` reached every session it hosted, an operator conversation included, whose Stop hook then stamped itself onto #146's attempt row and whose shell was policed by a worker's allowlist. The page collects what was scattered across src/hook.js, src/jobs.js and src/dispatch.js: the three answers to "which attempt is this session?", the order of trust between them, why the environment is scrubbed on that one launch (`scrubKbEnv`), why the judgement is narrow enough to leave `claude -p` and Actions workers untouched (`worksInWorktree`), and why the tick's job identity outranks a hook stamp. "Job record" and "Worker identity" added to the glossary)

## 2026-08-28 — refreshed architecture/overview (targeted, unstamped) for #150: the "which card" paragraph said the launch environment answers for a child process and the checkout for a background agent, which is still true but no longer complete — the two can disagree, and the checkout wins. One sentence, plus concepts/worker-identity in `related`. NOT re-stamped: the page was already stale for src/cli.js/src/tasks.js/src/doctor.js drift this task did not verify

## 2026-08-29 — refreshed concepts/worker-identity for the #150 review's B1 (a real edit, not a re-stamp: `attemptIdentity`'s "nothing here contradicts it" branch trusted the environment from *any* cwd that was neither the board root nor a mismatched `kb-<n>-<k>` checkout — a review worktree, or a session a poisoned daemon went on to host for an unrelated repo, both read as agreement. Fixed so only a `kb-<n>-<k>` checkout naming this exact task and attempt counts as evidence; everywhere else is a leak, the same as the board root always was. `checkEnvLeak` (src/doctor.js) inherits the fix for free — it calls the same function. New tests in test/hook.test.js for both shapes)

## 2026-08-28 — added features/operator-seat for #149 (the third slash command, `/kanban:operate`: the operator's seat written down as a procedure — up, watch, react per event kind, report — because it is the one seat hkb structurally cannot guard. The page carries what the SKILL.md section cannot: that every worker guard (`refuseIfWorker`, `DENY_PATTERNS`, the launch denies) tests `KB_TASK`, which an operator session does not have, so the boundary *is* the brief; that the reaction table is a projection of `EVENT_KINDS` / `STATUSES` / `OUTCOMES` / `BLOCK_KINDS` and a new test in test/skill.test.js fails when a token is added with no row; and the four rows that encode lifecycle behaviour a session would otherwise get wrong — comment before unblock, the two block kinds that ask for nobody, `manual` as a real answer, and request-changes as a relaunch. Glossary gained *Operator session* and an amended *Planning command*. No other page was re-stamped: the ones already stale at HEAD are stale for drift this task did not read)

## 2026-08-28 — refreshed features/planning-commands (targeted, unstamped) for #149: it said `commands/` "holds exactly two flat files", which this task made false — the directory now holds three, and the third is not a planning command, so the sentence names it and links the page that owns it. The two `SKILL.md` section citations (`:147`/`:175`) were re-read and corrected to `:322`/`:350`. NOT re-stamped: the page was already stale at HEAD for `src/init.js`, `src/doctor.js` and `scripts/smoke-pack.mjs` drift this task did not verify

## 2026-08-28 — refreshed features/hook-install-shapes (true no-op re-stamp) for #149: it was fresh at origin/main and only this task's own edits made it stale, all three of them outside anything it claims — two rows appended to `MUST_SHIP`/`FROM_PACKAGE` in `scripts/smoke-pack.mjs` for the new command file, and comment-only rewording in `src/init.js` (the `---------- the slash commands ----------` block) and `src/doctor.js` (`checkCommands`'s docstring). The page owns the hook command's shape, `hkbOnPath`, `checkHooks` and the settings file it lands in; none of that moved

## 2026-08-28 — refreshed features/hook-install-shapes for #144 (a real rewrite of the placement half, and a retraction of it: the page's central table used to answer "which settings file", and the answer is now *neither* — hkb's `Stop` and `PreToolUse` hooks serve only the worker hkb launched, so they ride that worker's launch as `--settings '{"hooks":…}'` and `hkb init` writes no settings file unless `--shared-hooks` asks. New sections: why the launch may name this machine when a tracked file may not (#85 inverted, not contradicted); why `.kanban/board.json` holds the `{hook_settings}` placeholder and never the JSON; the measured `claude --bg` forwarding answer, carried with a `TODO-VERIFY` because it was read out of the shipped 2.1.251 binary's own forwarding table rather than observed at runtime; and doctor's two new checks, `hooks in settings` and `launch hooks` — the latter for a launch frozen in `board.json` before the flag existed, which is the one way this change could go quiet. The command-shape half — measured-not-composed, the guard, the `hkbOnPath` trap, the two parser consequences — survives intact, rescoped to the file `--shared-hooks` writes; the guard's "cost to name in the worker prompt" is gone, because a launch names an hkb that is already installed. architecture/overview gained a paragraph and was NOT re-stamped: it is stale for #124/#127/#131 drift this task did not verify, as it was at origin/main)

## 2026-08-28 — refreshed features/up-and-down for #144 (a true no-op re-stamp: nothing on the page is about hooks, launches or `--settings`. Every anchor sat below one of this task's insertions and was shifted by the measured amount, then each subject re-read at its new line: `src/model.js` +43 (`detachedEnv` :827-831 → :870-874, `pidFileStale` :851-856 → :894-899, `stopWaitMs` :864-868 → :907-911, `processLine` :892-906 → :935-949, `startLogLine` :920-922 → :963-965), `src/board.js` +13 (`readPidFile` :176-183 → :189-196, `recordExit` :192-196 → :205-209, `processState` :212-226 → :225-239), `src/dispatch.js` +12 (`acquireLoopLock` :918-937 → :930-949 and :924/:933/:934-936 with it, `loop` :945-990 → :957-1002, the exit-4 record :991-1000 → :1003-1012, the worker `KB_*` export :128 → :140). `src/serve.js` and `src/up.js` were untouched by this task and their anchors were left)

## 2026-08-28 — refreshed features/web-board for #144 (a true no-op re-stamp for the same reason: this task's `src/board.js` change is the 13-line `HOOK_SETTINGS_VAR` block above `DEFAULT_PROFILES` plus one launch element each in three profiles, and its `src/init.js` change is the hook section — neither is a claim this page makes. Five anchors below those insertions were shifted and re-read: `registerUserBoard` `src/board.js`:525-531 → :538-544, `saveUserBoards` :487-499 → :500-512, `mainWorktree` :507-514 → :520-527, `registerCheckout` `src/init.js`:726-741 → :761-776, and init's last step :888 → :932. `src/serve.js` and `src/tasks.js` anchors were untouched)

## 2026-08-28 — merged origin/main (#144) into #149's branch: `features/hook-install-shapes` and `features/web-board` both took **main's** version whole, since #144 rewrote the first and re-anchored the second against line numbers this branch never saw; the two no-op re-stamps this branch logged above are superseded by those. `features/operator-seat` and `features/planning-commands` are this branch's and were kept. Both surviving pages re-checked after the merge

## 2026-08-28 — refreshed features/hook-install-shapes for #166 (a real addition, not a re-stamp: `installHarness` and `mcpLaunch` — the Codex/Copilot hook writer and the `.mcp.json` writer — called `hkbCommandForHook()`/`projectBinRel()` with no `root`, so on a devDependency or self-checkout repo they always fell through to the machine-specific branch and wrote an absolute path into three *tracked* files, exactly the failure #146 fixed for Claude's settings file but not reached from these two call sites. New section "Codex, Copilot and `.mcp.json` get the same remainder, unguarded (#166)": the same `projectBinRel` remainder, named relative to each harness's own cwd (`relativeHookCommand`, `hkbCommandForHook(verb, { cwd: true })`) instead of through `$CLAUDE_PROJECT_DIR`, since neither harness sets that variable but both already run their hook from the project root; and why it ships with no `[ -f … ] || exit 0` guard — the guard's `f="…";…` is shell syntax, and whether either harness runs `command` through a shell is undocumented, so the unguarded form is the one correct either way, at the cost of a hard failure instead of silence before `npm ci`. `src/mcp.js` added to `covers`; every other citation on the page was re-read and had not moved)
## 2026-08-28 — refreshed features/review-loop and features/web-board for #162 (three gaps #160's review of #153 found, closed: `worktreeOnBranch` reused the *local* branch as `git worktree add` left it, so a human push to the PR since the last attempt was invisible and the continuation's own `git push` would be rejected non-fast-forward — it now runs `git merge --ff-only origin/<branch>` right after the add, best effort and never a force, and reports a genuine divergence as `stale` rather than claiming the checkout current; `spawnWorker` turns that into `continues_branch_stale` on the attempt row and a third, accurate brief variant — checked out on the right branch but not caught up — instead of either the "already there" line or the fresh-branch recipe. Trigger-mode profiles (`claude-action`) never made this checkout at all: the real worker runs elsewhere, on its own `actions/checkout`, so `spawnWorker` now skips `worktreeOnBranch` outright when `profile.mode === 'trigger'` and such an attempt records `continues_pr` only. The page's "Known gaps" section and the checkout paragraphs were rewritten for all three; the *Known gaps* fetch-failure line now names what the ff-only merge does with a fetch that silently failed. features/web-board was a true no-op re-stamp — its `src/board.js` sha moved for `worktreeOnBranch` alone, and the page claims nothing about it. Both back to fresh; the board-wide baseline returns to the 7 pages stale at main before this task)

## 2026-08-28 — refreshed features/operator-seat for #162 (a true no-op re-stamp, but one anchor had actually drifted: this task's `activePrGuard` rewrite in `src/model.js` inserted 6 lines above `DENY_PATTERNS`, moving it from `:724-732` to `:729-736` — the citation was corrected. `src/cli.js`'s `refuseIfWorker:235-238` and `src/lifecycle.js`'s three request-changes/block citations all sit above where this task edited and were re-checked in place, unmoved. No claim on the page is about `activePrGuard`, `claim --spawn`'s new `continuePr` handling, or the CLI wording fix)

## 2026-08-29 — merged origin/main (#144/#149) into #150's branch (PR #152): mechanical conflicts only, no semantic clash — `docs/harnesses.md`'s hook table gained both a "where it comes from" row (#144) and #150's "and the checkout when the two disagree" wording on "what identifies the session"; `src/dispatch.js` kept both import additions (`HOOK_SETTINGS_VAR`/`workerHookSettings` from #144, `scrubKbEnv` from #150 — #144 passes hook settings on argv, not via `KB_*`, so the scrub does not strip it); this file's two branches of entries were concatenated in date order

## 2026-08-29 — refreshed concepts/worker-identity for the #150 review's second pass (real edit: the review's probe — cwd `kb-149-3` with `KB_ROOT=/nonexistent` — found `attemptIdentity`'s shipped rule only compared checkout *basenames*, while this page and `docs/harnesses.md` already claimed the stronger "KB_ROOT joined with kb-<n>-<k> is the only cwd that counts". Implemented the exact compare rather than retracting the claim: `attemptIdentity` (`src/model.js`) now takes `herePath`/`rootPath` — the caller's `path.resolve`d cwd and `KB_ROOT` — and only agrees when the resolved cwd equals `rootPath` joined with `kb-<n>-<k>`; a same-numbered checkout under the wrong (or unset) `KB_ROOT` still falls back to `source: 'worktree'` with a leak note, never `'env'`, so the policy stays inert. `whichAttempt` (`src/hook.js`) and `checkEnvLeak` (`src/doctor.js`) both pass the new params; `docs/harnesses.md`'s claim needed no change, it was already accurate. New tests in test/hook.test.js for the wrong-KB_ROOT and unset-KB_ROOT shapes, at both the pure-function and doctor layers)

## 2026-08-29 — merged origin/main into #166's branch (PR #173): mechanical conflict on this file only (both branches appended a line, concatenated in date order); `src/doctor.js`, `docs/harnesses.md` and `docs/wiki/architecture/overview.md` auto-merged cleanly against #150's and #149's landed changes, no semantic clash with this task's `checkHarnesses`/`checkMcp` additions

## 2026-08-29 — refreshed features/web-board (no-op re-stamp for #150/#166 review pass) — `registerCheckout`'s two `src/init.js` anchors had drifted with unrelated insertions above them (`:769-784` → `:803-818`, `:940` → `:988`); the four `src/board.js`/`src/serve.js` anchors were re-read and had not moved. No claim on the page changed

## 2026-08-29 — refreshed features/hook-install-shapes for the #166 review's second pass (a real edit: `mcpLaunch`'s fallback used to be the same absolute, this-machine-only form `hkbCommandForHook`'s private branch writes, which the page said plainly — "instead of the absolute fallback" — but was never true for a *tracked* `.mcp.json`, only for the launch line. Fixed in code to match: `mcpLaunch` now checks the remainder in the same order as `hkbCommandForHook` (repo-carried hkb first, even over a global one on PATH) and falls back to the plain `hkb` every teammate needs on PATH, same as a harness file's `shared` branch — never an absolute path into a tracked file. New section on `mcpSnippets`: `.vscode/mcp.json` reuses `.mcp.json`'s launch since VS Code resolves it the same way, but `~/.codex/config.toml` is user-level and Codex resolves its `args` against wherever `codex` starts, not the project, so it gets its own `mcpLaunch({ shared: false })` — bare `hkb` or this checkout's own absolute `bin/hkb.js`, the one place that absolute form is still correct. `checkMcp`'s comment on the doctor side was corrected the same way. No line-number anchors in this section to re-check — it cites by function name only)
## 2026-08-29 — refreshed features/review-loop and features/operator-seat for #162's continuation review (merging origin/main — #144/#149/#150/#164 — moved both pages' anchors again). `review-loop`: a real content update, not just anchors — the reuse-existing-worktree path (a spawn that died between checkout and launch) now fetches and fast-forwards too, `worktreeOnBranch` distinguishes "no `<remote>/<branch>` ref to catch up to" (a fetch that never ran or never succeeded once) from a genuine divergence via `git rev-parse --verify --quiet` before the ff-only merge, `hkb claim --spawn` (`src/cli.js`) now records `continues_branch_stale` like the dispatcher's own claim, and the trigger-mode claim log line still names the PR it continues even though it makes no checkout. The Known-gaps sentence about a silent fetch failure was corrected: measured, a failed fetch against an *existing* tracking ref lets the ff-only merge succeed trivially ("Already up to date") and the checkout is reported clean though the remote may be ahead — only a fetch that has *never* created the ref is reported `stale`, and now with its own message rather than the generic ff-only failure. `src/cli.js` added to `covers`. `operator-seat`: a true no-op re-stamp — `refuseIfWorker:235-238` (`src/cli.js`) is unmoved, `DENY_PATTERNS` moved `:729-736` → `:838-846`, and the three `src/lifecycle.js` block/unblock citations shifted a few lines (`:283-293`→`:285-295`, `:294-299`→`:296-301`, `:302-313`→`:304-314`) for insertions above them elsewhere in the file; no claim on the page changed. features/web-board re-stamped again, no-op — its `src/board.js` sha moved for the same `worktreeOnBranch` edit and the page still claims nothing about it

## 2026-08-29 — refreshed features/hook-install-shapes for #182: `effort` joins `model` as a profile field rendered into `{model_args}` (`EFFORT_LEVELS`/`modelArgs` in `src/model.js`, validated in `loadBoard`, `src/board.js`), so a launch is never pinned just to set either. `launch hooks`'s fix (`src/doctor.js` `checkHooks`) now names the surgical repair — insert `{hook_settings}` after `--disallowedTools`, or drop the pin only if it added nothing but --model/--effort — instead of the old blanket "drop launch". No line-number anchors touched; the page cites by function/flag name only

## 2026-08-29 — added features/tracks for #129 (the page plan's long-planned track page, written at the moment the engine changed shape): a track root is now an ORCHESTRATOR — `Agent` is allow-listed on `claude-track` and no other profile (`CLAUDE_TRACK_TOOLS`, `src/board.js`), `trackFanout` (`src/track.js`) reads that list, and `trackContext({fanout:true})` tells the runner to claim a whole wave and spawn one isolated subagent per node in a single message instead of walking the nodes one at a time. The page carries what code cannot show: the 2026-08-28 spike's measured harness facts (child worktree is a repo-level `agent-<id>` sibling removed on return, so a node subagent must push before it finishes; the child inherits the root's session id, keeping the counted-once rule; `--settings` hooks including PreToolUse fire in children; the worktree guard refuses `;`/`$VAR` in root and child; `Stop` fires while subagents run, which is what #163 answered), plus the three known gaps — the verb check is brief-level not enforced, `hkb stats` cannot see sidecar subagent transcripts (#155), and nothing re-checks `kb.paths` disjointness at wave time. GLOSSARY gained **Wave** and an amended **Track**; `wiki.config.yml` marks the slug seeded. The 11 pages already stale from #144/#149-era drift are untouched — this card did not own them

## 2026-08-29 — refreshed features/tracks for #197 (the sentences the fan-out brief was still missing, from the PR #194/#129 review): the per-node brief now tells each subagent to run `hkb heartbeat <root>` every ~10 minutes — the orchestrator's own turn ends the moment it spawns a wave and it cannot wake itself (`ScheduleWakeup` is not allow-listed), so the beat has to come from inside the child instead — and overrides `hkb context <n>`'s current-branch line, which names the child's own throwaway `agent-<id>` checkout rather than `kb/<n>`. The wave loop's spawn step now names the fallback when `Agent` itself errors: work the node yourself in this checkout, sequentially, or `hkb block <n> --kind transient` to release it, rather than leaving it claimed with nobody on it. `src/gc.js` gained `agentWorktreeNode`/`sweepAgentWorktrees`: an `agent-<id>` worktree a subagent left behind (it committed, so the harness did not auto-remove it) is now swept once its `kb/<n>` branch's PR is merged or closed, both from `hkb gc` and from `sweepTask` the moment its own task settles — closing the gap where these accumulated forever because `attemptOf` only ever recognised `kb-<n>-<k>`/`worktree-kb-<n>-<k>`. Softened the "worktree is deleted when you finish" line in the brief and in `docs/harnesses.md` to match: only true when the child leaves it unchanged. New Known-gaps bullets: the stale-reclaim risk when a subagent skips its heartbeat, and the daemon-leaked `KB_TASK` (#150) that can suppress the root's own `Stop` via `attemptIdentity` accepting `profile: null`

## 2026-08-28 — refreshed features/up-and-down for #177 (#164's second-pass review): real edits, not just re-anchoring. `down`'s "not running" tidy and `up`'s dead-at-recheck tidy now share `dropDeadPidFile` (`src/up.js`), which takes a *fresh* pid-file read right before `rmSync` rather than trusting the `processState`/liveness snapshot the caller already acted on — a concurrent `hkb up` or the process's own startup claim rewriting the file to a live pid in that window now survives both tidy paths, closing the one gap the "pid file is never dropped while it names something alive" invariant still had. `up` also now tidies `serve.pid` for the corpse it just reported `failed`, guarded the same way and only when it still owns that claim. `down`'s stale-pid line says `removed` when it actually removed the file, instead of reusing `--status`'s `hkb up replaces it` wording. `deadAtRecheck` (`src/model.js`) now returns `{line, failed}` directly instead of a flat shape `up.js` re-picked apart, with its own unit test. Every line anchor in the page re-verified against current line numbers, including the churn `#162`'s continuation review left in `src/dispatch.js` and `src/model.js` above this page's own edits (`src/up.js` untouched by that merge); added a Known limits bullet for the 300ms `SPAWN_CHECK_MS` window a child can still die just past. New tests: the injected-read race for `down`, the stale-removed message, `deadAtRecheck`'s shape, and `up --serve --port 80` leaving no `serve.pid` behind a dead recheck

## 2026-08-29 — added features/path-overlap-guard for #185 (third pass, the review's "add a short features page or a section on an existing one" ask): documents the three `dispatch.guards.path_overlap` modes (`off`/`running`/`unmerged`), `pathOverlapGuard`'s precedence ladder including the new `merge.mode: "operator"` (#189) falling back to `"off"` like `"manual"`, and the idle-liveness ladder this same card's code changes added — job record, then live pid, then a lock-ref beat refreshed at the (now `Math.max(interval, 1200)`) idle threshold instead of only at `stale_after`. `wiki.config.yml` gained the slug (seeded); index regenerated
## 2026-08-29 — refreshed features/hook-install-shapes for #188 (both halves of #182's review shipped): `staleHookLaunches` moved from `src/doctor.js` to `src/board.js` so `hkb init` can call the same question doctor only reports; `init` now repairs a stale pin itself (`repairLaunchHooks`, `src/init.js`) instead of leaving it broken and silent, printing one line per profile it touches. `loadBoard` also now refuses `effort` on any profile whose `launch[0]` is not `claude` (`claude-action` excepted), since Codex and Copilot CLI have no verified `--effort` flag and used to only fail on the CLI's own error at first spawn. Prose in the `launch hooks` bullet updated to match; citations re-verified. `architecture/overview`, `concepts/worker-identity`, `decisions/adr-004-roles-and-adoption`, `features/auto-merge`, `features/operator-seat`, `features/planning-commands`, `features/review-loop`, `features/tracks`, `features/up-and-down`, `features/update-notice` and `features/web-board` went stale from the same `src/board.js`/`src/doctor.js`/`src/init.js` edits but assert nothing this change touches — re-stamped with no content edits, after checking each one's citations of those files against the diff

## 2026-09-01 — refreshed features/up-and-down for #205 (WSL2 pid-staleness false positive): `pidFileStale` now derives boot from a kernel-reported instant when one exists (`bootInstantMs` preferring `/proc/stat`'s `btime` or macOS `kern.boottime`), but the actual fix is corroboration — `pidClaimStale`/`cmdlineIsOurs` rescue a stale verdict only when the pid is alive and `/proc/<pid>/cmdline` still names our own `hkb dispatch --loop`/`hkb serve`, never manufacturing staleness the timestamp verdict didn't already reach, so the #202 reused-pid direction still fails closed. Lives entirely in `readPidFile` (`src/board.js`), so every caller (`processState`, the dispatcher's singleton lock, the server's claim, `hkb up`'s spawn gate) is fixed without their own changes

## 2026-09-01 — refreshed architecture/overview for #207 (documented the priority band): `--priority`'s direction was documented four times with no scale, and the shipped decompose example gave a documentation card (`p3`) a higher number than the token bucket it documents (`p2`) — readers learn the convention from the worked example, not the sentence. The dispatcher paragraph now names the band README.md defines (`0` unfiled default · `1` normal · `2` next up · `3` urgent) and is explicit that `sortReady` (`src/model.js`) only ever compares the raw integer, never the band. `README.md`, `skills/kanban/references/protocol.md` and `skills/kanban/SKILL.md` were fixed to state the same band once (README) and reference it from the other three, including `hkb create --help`; both worked examples (protocol.md's inline one and SKILL.md's decompose table + walkthrough) now give the foundation card (`#41`/A, `p2`) a higher priority than the card documenting it (`#43`/C, now `p1`). No `sortReady` behaviour changed. "Priority band" added to the glossary
## 2026-09-01 — refreshed features/tracks for #161 (a root with children is a track by default): new section "Who is a root: the graph decides, the label overrides" documents `isTrackRoot` (`src/model.js`, pure) and its four modes — forced (the card's own profile has `"track": true`), opted-out (`kb:no-track`), inferred (≥1 unfinished child, nothing on the board still blocked by it, some track profile's `track_agents` can execute its profile) and none. Two claims worth the page: the `kb:agent:*` label is never rewritten, so the tick takes the launch profile from the decision and counts the running track's slot against *that* profile's cap; and only the outermost root is inferred, which is why `board` is a parameter and why `hkb show` (one issue, no board) answers in isolation. `planTracks`/`trackReadiness` lines updated to match, and a known gap added — a root `running` for any other reason still covers its nodes, true before this change for `claude-track` roots and now true for every inferred one. `src/model.js` added to `covers`; GLOSSARY gained **Track root** and an amended **Track**
## 2026-09-01 — refreshed features/web-board for #209 (promote no longer strands a root's blockers in triage): `promote` (`src/lifecycle.js`) now resolves the dragged/typed card's whole subgraph (`resolveTrack`, `src/track.js`) and walks it blockers-first, so a root with open triage blockers sweeps them to `todo` in the same call instead of landing alone in a `todo` the tick can never ready. `promoteDecision` (`src/model.js`) is the new pure per-card rule: a genuine leaf (no open blockers) keeps today's forcing behaviour, but every card swept up by a real cascade only advances when actually ready, and a `blocked` card is left for the human rather than guessed at. Added a "Promote cascades forward, never backward" section citing the new call sites; `src/lifecycle.js`, `src/track.js` and `src/model.js` added to `covers`
## 2026-09-01 — refreshed features/tracks for #161 (review round 2): `kb:no-track` is now a real board label — `hkb init` creates it alongside the statuses (`src/init.js`) and `hkb doctor`'s `labels` check lists it as missing, so a board set up before inference landed is told to re-run init rather than left with an opt-out that only exists once a human hand-makes it in the GitHub UI. `covers` gained `src/init.js`; shas refreshed after merging `origin/main` (#209's `promoteDecision` landed next to `isTrackRoot` in `src/model.js`)

## 2026-09-01 — refreshed architecture/overview for #155 (read the whole result, not just cost): `parseSessionLog` (`src/model.js`) now keeps `terminal_reason`, `api_error_status`, `model_usage` (aliased from the result's own camelCase `modelUsage`) and `permission_denials` alongside `session_id`/`total_cost_usd`/`num_turns`/`duration_ms`, all still truncated to `SESSION_FIELDS` so an old log with none of them still parses unchanged. The session-identity paragraph gained a sentence on the pid-mode tick backfill this unlocked (`src/dispatch.js`, right before `failAttempt`, the same treatment `jobSessionUpdate` already gave a `--bg` row) and a new paragraph on `watchChild` reading `api_error_status` to pause a profile on 401/429, with the old log-tail regex now a fallback only for a log with no JSON result line at all.

## 2026-09-01 — added features/backlog-grooming (track #191 shipped `hkb groom` and `/kanban:groom` across five node PRs — #230, #231, #232, #233, #235): the page is the whole-feature story no single child PR could write. It records the cut between the arithmetic half (`GROOM_KINDS`, `GROOM_ACTIONS`, `groomBoard` and the pure helpers in `src/model.js`) and the judgment half (the `## /kanban:groom` section in `skills/kanban/SKILL.md` plus `commands/groom.md`), why a finding's level says *who is competent to decide* rather than *how bad it is*, and the three false-positive guards a later change is most likely to break: hub removal before the Jaccard score (`pathHubs`/`pathJaccard`), the guard-aware `overlap_pair` wording that never claims two cards will serialize when `dispatch.guards.path_overlap` is off, and unknown-vs-empty blockers read through `blockersOf`/`blockersKnown` (`src/tasks.js`). Verified live rather than asserted: `hkb groom --json` against this board made exactly one `gh api graphql` invocation and zero writes. Known gaps recorded in the page rather than fixed — `hkb edit` is named by four finding kinds' `suggests` strings but is not a `case` in `src/cli.js`, `hkb promote --triage-only` does not exist, and there is no close-as-duplicate verb. `features/planning-commands` re-summarised from two planning commands to three in the page plan; `GLOSSARY.md` gained *Grooming*, *Finding / finding level*, *Groom action* and *Hub path*, and its *Planning command* line now counts three.
## 2026-09-01 — added a paragraph to architecture/overview.md for #140 (a `dispatch --loop` outliving the code it started with): `loop` (`src/dispatch.js`) now compares `installStamp()` — the checkout's own `git rev-parse HEAD` when `PKG_ROOT` is one, the package version otherwise, both read fresh off disk every tick, never cached — against the stamp it captured at startup, and exits 4 (the same self-heal code) on a mismatch, local `git` only, no GitHub call. Not stamped fresh: the page was already stale for this and six other covered files from unrelated drift the parent tasks (#124/#127/#131/#209/#155) left as owed to `/repolore:refresh`, and re-stamping here would bless citations this task did not re-verify.

## 2026-09-01 — refreshed features/backlog-grooming for #238 (`hkb promote --triage-only` now exists): the known-gaps note it was missing is removed, the batch example in `skills/kanban/SKILL.md` and the procedure paragraph both say `hkb promote --triage-only` instead of bare `hkb promote`, and `src/lifecycle.js` joins `covers` — the guard (a card no longer in *triage* is skipped before anything is read or written, reported as skipped) lives in `promote()` there, not in `src/model.js`.

## 2026-09-01 — refreshed features/tracks and features/review-loop for #234 (the PR head-branch fallback): #227 and #228, found operating the #191 track, each completed to *done* with a PR left open and unmerged because `closedByPullRequestsReferences` never linked it (#227's targeted an intermediate `kb/191-wave1` branch — the track runner's own documented stacking, `git switch -c kb/<n> <base>`; #228's cause was never established even against the default branch). `src/tasks.js` now falls back to a board-wide open-PR listing matched by head branch (`taskBranchRe`/`openPrsByHead`/`branchFallbackPrs`) whenever a card's own `prs` comes back empty — one request per tick, not per card — and `hkb finish` (`noPrDecision`, `src/lifecycle.js`) refuses to land a card in *done* with no PR found at all, recording `protocol_violation` instead unless the worker passes `--no-pr "why"`. `features/tracks` gained a "Branch strategy: nodes stack on their blocker, and the board can see it" section stating the stacking is deliberate (not the bug) and that the track runner never creates a branch of its own outside `taskBranchRe`'s coverage; `features/review-loop` gained "Finding the PR at all" describing the fallback from the guard/merge-policy side. `hkb doctor` gained `checkOrphanedPrs` for the one gap the live fallback cannot reach: a card already closed before this shipped, whose branch still carries an open PR an open-issues board read never revisits. `src/tasks.js` and `src/doctor.js` added to both pages' `covers`.



## 2026-09-01 — refreshed thirteen living pages after a 21-merge day. The drift was overwhelmingly bookkeeping rather than prose: each merge re-hashed a file another page covered, so pages the day's own workers had already corrected kept reporting stale. Verified before stamping rather than stamping to clear the count — `features/up-and-down` already described `pidClaimStale`'s `/proc` corroboration and the `btime`/`os.uptime()` fallback (#205), `features/auto-merge` already carried `merge.mode: "operator"` (#189), `features/review-loop` already had the PR head-branch fallback (#234), `features/web-board` already described `promote` moving a subgraph and never forcing a blocker (#209), and `architecture/overview` already noted that a loop current with its checkout pays nothing per tick for `installStamp` (#140). One page carried a genuine gap: `concepts/worker-identity` explained *which* session an attempt is but not what the stamp now carries, so it gained a paragraph on `SESSION_FIELDS` growing `terminal_reason` / `api_error_status` / `model_usage` / `permission_denials` (#155) — what that removes is needing the transcript to learn why an attempt ended, the one thing a post-mortem cannot do once the session is gone — plus the two traps behind it, `modelUsage` being camelCase alone among its siblings and the by-value comparison two of the four force on `sessionUpdate`. `decisions/adr-004-roles-and-adoption` was deliberately **left stale**: accepted decisions are immutable-with-supersession, and a stale one is a prompt to consider a superseding record, not to edit it. Whether today supersedes it is a human's call — the candidate is its "operator = the human" seat definition against `merge.mode: "operator"` (#189), where a session now holds that seat under a delegation written into `board.json`.

## 2026-09-01 — refreshed features/tracks for #245: a track now claims its own integration branch, `kb/track-<root>` (`trackBranchName`, `src/model.js`; `ensureTrackBranch`, `src/lock.js`), from the default branch at claim time, recorded on the root's attempt row (`track_branch`) before anything spawns. Every node branches from it instead of its blocker — the diamond that lost #227's work and the sibling-chain that let #229's base be squash-merged out from under it both go away — and the root's own pass runs on the track branch and opens the track's one, GitHub-linkable PR into the default branch. The "Branch strategy" section was rewritten in place (the old sibling-stacking model it described is gone, not superseded prose); it gained "Conflict on the way into the track branch is an event, not a discovery" (`trackConflictPass`/`trackBranchConflict`, one GraphQL `mergeable`/`mergeStateStatus` batch read, surfaced as `kb:needs-human` + a comment, once per attempt) and "Lifecycle" (`sweepTrackBranches` in `hkb gc` deletes a settled root's branch; `checkTrackBranches` in `hkb doctor` flags one with no live runner). `src/lock.js` added to `covers`.

## 2026-09-01 — added features/denied-tools-ledger for #130. Workers under `--permission-mode dontAsk` were denied 208 times across 56 of 71 attempts on one board and routed around every one silently; the new `denied_tools` ledger (`buildDeniedTools`, `src/model.js`) merges the `--disallowedTools` shape #155 already reads out of `permission_denials` with two shapes only a transcript carries — a dontAsk allowlist miss and the worktree guard's tool-result error (`parseTranscriptDenials`, `src/stats.js`). Read at the Stop hook (`writeSession`, `src/hook.js`) and, for a pid-mode attempt whose Stop never fired, at dispatch reclaim (`attachDeniedTools`, `src/dispatch.js`). Surfaced in `hkb stats` (a board-wide `denied` line) and `hkb doctor` (`checkDeniedTools`: the most-denied tool becomes the exact fix, plus whether a repo `.mcp.json` server ever reached a worker session at all). `hkb show` and the serve drawer are not wired yet — `src/cli.js`/`src/serve.js` were claimed by #204 at the time this landed; `formatDeniedTools` (`src/model.js`) is ready as a drop-in.

## 2026-09-01 — added features/capability-map for track #217 (children #255, #259, #260, #261). A profile now says what it reaches for inside a session, not only how to start one: a frozen `CAPABILITIES` intent vocabulary and `capabilityCommand` (`src/model.js`), a board-local `profile.capabilities` binding validated by `loadBoard` (`src/board.js`), one line per triggered intent in the worker brief (`briefIntents`/`capabilityLine`, `src/context.js`), the implied tool grant derived inside `effectiveTools` via `capabilityGrants`/`grantWithCapabilities` and nowhere else (`src/model.js`), a `capability map` check that asks that function rather than recomputing (`src/doctor.js`), and an operator `review` row that runs the declared capability alongside *Done when* (`skills/kanban/SKILL.md`). The seam (`effectiveTools`, #255) shipped first and alone so this feature and the tool posture (#223) plug into one derivation rather than each growing its own. Verified live on this board: binding `review`/`goal` on the `claude` profile adds one `hkb context` line and two `hkb doctor` lines, and unbinding restores both outputs byte for byte. Two gaps recorded on the page rather than fixed — a card narrowing `kb.tools` can strip the derived grant with nothing reporting it (the removal is not in `dropped`), and `trackFanout` (`src/track.js`) plus the Stop hook (`src/hook.js`) still read `allowed_tools` raw, so neither sees a narrowing or a derived grant. Complements `concepts/capability-portability`, the contract page added by #261.
## 2026-09-01 — refreshed concepts/worker-identity and features/denied-tools-ledger for #254. On `french-tech-homies/pipao-v2` no worker had ever loaded an MCP server across the board's whole life: `react-aria`/`supabase`/`vercel` were defined in `.mcp.json` and granted in both profiles' `allowed_tools`, but approved only in the gitignored `.claude/settings.local.json` — a worktree carries tracked files only, so that approval never reached one, and #130's `mcp visibility` check could only say "never showed up", pointing at the wrong file (`.mcp.json`, not the settings split). `mcpApproved`/`mcpVisibilityDiagnosis`/`mcpSplitApprovals` (`src/model.js`) now read `.mcp.json`, a profile's `allowed_tools` and both settings files together and distinguish `local-only` (never approved for a worktree — names the exact line and the tracked file to move it to) from `unused` (approved in the tracked file, so it did reach a worktree — a different bug) from `unapproved` (granted, approved nowhere). `checkDeniedTools` (`src/doctor.js`) uses this before falling back to the old generic wording; `hkb init` (`src/init.js`) reports the same `local-only` split at setup, before a worker ever runs.
## 2026-09-02 — refreshed features/capability-map and features/tracks for #273, closing the gap the capability-map page recorded on 2026-09-01: `trackFanout` (`src/track.js`) took an optional `task` and now derives its `Agent` check from `effectiveTools(profile, task, cfg)` instead of the profile's raw `allowed_tools`, so a root card's own `kb.tools` narrowing is honoured — `src/dispatch.js`'s one call site passes the root task. The Stop hook's `preToolHook` (`src/hook.js`) now derives `allowedCmds` from `effectiveTools(profile, null, ctx.cfg).tools`, so a capability-derived grant is on its radar; it still passes no `task` because it fires once per tool call and fetching the card there would cost a request per call rather than per session, so a card's `kb.tools`/`kb.mcp` narrowing remains invisible to it — noted on the page as the residual half of the gap. `concepts/worker-identity` gained "A worktree carries no developer approvals either (#254)" stating the general rule this is one instance of; `features/denied-tools-ledger` gained the mechanism paragraph and `src/init.js` in `covers`.
## 2026-09-01 — added concepts/tool-grant-ceiling for #258 (track #223). States the ceiling rule in one sentence — the board is the ceiling, a card lowers it, only a human editing `.kanban/board.json` raises it — and names the three places it lives: `effectiveTools` (`src/model.js`) as the sole enforcement, `normalizeCardGrants` (`src/tasks.js`) as the path `kb.tools`/`kb.mcp` enter on, and `checkToolPosture`/`checkCardGrants` (`src/doctor.js`), which print each profile's posture, ceiling and MCP answer and flag an open card asking for what its profile does not grant. Complements `concepts/capability-portability`: the map widens a profile, the card can only narrow what comes out.
