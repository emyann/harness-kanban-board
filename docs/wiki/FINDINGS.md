# Findings inbox

One line per suspected code defect, awaiting triage. Not a tracker: every item leaves through one of
four exits — fixed now, promoted into the page that owns it, filed in the issue tracker, or dismissed
with a reason — and the departure gets a `log.md` line. Treat every item as a **claim to re-verify**,
not a fact. Grammar and the triage rules are in `AGENTS.md`.

- [bug] **`hkb groom` reports `blockers_source` from GitHub's capability probe, not from the board** — `groomBoard` computes it as `caps.blockedByGql ? 'graphql' : (blockersFilled ? 'rest' : 'unknown')`, so a board read through any non-GitHub store reports `blockers from rest` for a board that made no REST call. `listTasks` already hangs the provenance on the array it returns (`tagBlockers` / `blockersOf`, the pair that exists so an answer about provenance reads the same whichever store produced it) and `cli.js` already reads `blockersOf(tasks).filled` from it two lines away — the `source` should come from the same place. Evidence `src/model.js:1272`, `src/cli.js:568-580`; asserted-as-it-behaves in `test/cli.test.js` ('hkb groom --json') <!-- repolore:unanchored captured=2026-09-03 --> → architecture/store-seam
- [bug] **the dispatcher names a GitHub ref on every board** — `src/dispatch.js:200` puts `lockRef(task.number, attempt)` (`refs/kb/locks/<n>/<k>`, `src/model.js`) in `KB_LOCK_REF` for the worker, and line 727 prints "attempt 1 beat on refs/kb/locks/7/1" in the tick log. The interface grew `lockRef(n, k)` for exactly this in #325 — a store that keeps its claims in a table answers `null` and callers fall back to the attempt number — and `hkb heartbeat`, `hkb claim` and the LOCK_LOST error were moved onto it; these two sites were not. Evidence `src/dispatch.js:200,727` <!-- repolore:unanchored captured=2026-09-03 --> → architecture/store-seam
- [cleanup] **`hkb init` needs a forge even for a purely local board** — `init` resolves the repo through `gh repo view` and refuses a checkout with no remote (`no git remotes found`), so the store that exists to work with `gh` logged out cannot be created without one. Evidence `src/init.js` (`detectRepo` / the repo step) <!-- repolore:unanchored captured=2026-09-03 --> → architecture/local-store
