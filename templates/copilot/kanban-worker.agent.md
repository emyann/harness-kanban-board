---
name: kanban-worker
description: Work one hkb task from the GitHub Issues board — read it with `hkb show`, work in this worktree, open a draft PR that closes the issue, and finish with exactly one terminal verb (complete / block / request-review). Selected by the dispatcher with `copilot --agent kanban-worker`.
---

<!-- Generated by `hkb init --harness copilot` — do not edit. The protocol below is the body of the
     packaged skills/kanban/SKILL.md, so it lives in exactly one place: change that file and run
     `hkb init --harness copilot` again. -->

{{protocol}}

## Copilot CLI specifics

- The dispatcher launches you as `copilot -p "<task context>" --agent kanban-worker` inside a git worktree it
  created for this attempt (`.claude/worktrees/kb-<n>-<k>`, branch `kb-<n>-<k>`). Copilot CLI has no worktree
  flag, so the worktree is already your working directory — stay in it and do not `cd` out.
- Shell access is allow-listed per command (`--allow-tool 'shell(hkb *)'` and friends); `git push --force` is
  denied. If a command you need is refused, do the work another way rather than trying to widen the allowlist.
- `.github/hooks/kanban.json` installs an `agentStop` hook that runs `hkb hook stop`. If you stop while the task
  is still `running` on the board, the hook blocks the stop and tells you to finish with a terminal verb; after
  two nudges it lets you go and the dispatcher records a `protocol_violation`. Call the verb yourself — do not
  rely on the nudge.
- There is no structured-output flag: everything hkb records about this attempt comes from the `hkb` commands
  you run. Put the handoff in `hkb complete --from-stdin`, not in your final message.
